# 个人工具箱 -- 开发规范与经验总结

> 适用环境：Python 3.x + Flask + SQLite，部署于 PythonAnywhere 免费云服务。  
> 目的：避免重复踩坑，保证新模块开发一致性，确保部署流程可靠。

---

## 一、项目架构总览

```
app.py                     # Flask 主入口：认证、路由注册、启动初始化
├─ routes/utils.py         # 共用工具函数（数据库、日志、时区、加密）
├─ routes/deploy.py        # 部署工具 （git-pull、状态监控）
├─ routes/backup.py        # 自动备份清理
├─ routes/renqing.py       # 人情礼金系统
├─ routes/gpa.py           # GPA成绩系统
├─ routes/hsgrades.py      # 高中成绩系统
├─ routes/countdown.py     # 倒计时系统（公历/农历）
├─ routes/accounting.py    # 个人记账系统（收支/借贷管理）
├─ routes/ledger.py        # 专业记账会计平台（复式凭证/账簿/报表）
├─ routes/nav.py           # 导航页
├─ routes/pa.py            # PA 自动续期

各模块前端 HTML 存放在对应中文目录下：
人情/  绩点/  成绩/  倒计时/  记账/  会计/  部署/  导航/
```

**核心原则**：
- 每个业务模块 = 1 个 Blueprint + 1 个 HTML 前端 + 1 个 SQLite 数据库
- 模块之间完全独立，不相互导入
- 前端为纯原生 HTML/CSS/JS（无框架、无构建工具）
- 部署 API 优于一切业务模块，确保即使某模块崩溃也能远程恢复

---

## 二、代码规范

### 2.1 Python 缩进

**必须使用 4 个空格缩进，严禁使用 Tab 或混用。**

这是本项目最惨痛的教训：`countdown.py` 中 `create_event()` 函数体无意缩进了 8 个空格，导致 `IndentationError`，整个 Flask 应用崩溃，部署 API 也一起挂掉，形成死锁。

**预防措施**：
- 编写代码后立即执行 `python -m py_compile xxx.py` 检查语法
- 提交前运行 `python -c "from routes.xxx import bp; print('OK')"` 验证导入
- 使用编辑器的「显示空白字符」功能检查缩进

### 2.2 文件头声明

每个 `.py` 文件必须包含：

```python
# -*- coding: utf-8 -*-
```

### 2.3 导入顺序

```python
# 1. 标准库
import os, json, time
from datetime import datetime

# 2. 第三方库
from flask import Blueprint, jsonify, request

# 3. 项目内导入（相对导入）
from .utils import TZ, make_logger, make_db
```

### 2.4 异常处理

**每个 API 路由必须用 try/except 包裹，返回统一 JSON 格式：**

```python
@bp.route('/api/xxx/action', methods=['POST'])
def action():
    try:
        # 业务逻辑
        return jsonify({'success': True, 'data': result})
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 500
```

### 2.5 日志规范

使用 `utils.make_logger()` 创建模块级日志：

```python
from .utils import make_logger
_log = make_logger(os.path.join(BASE_DIR, '目录', '模块名.log'))

def _log(msg):
    """简单日志，带时间戳"""
    # make_logger 自动处理目录创建和时间戳
```

### 2.6 敏感配置管理

**Token、密钥、密码等敏感值禁止硬编码在代码中。** 统一使用环境变量 + 默认值兜底模式：

```python
# 正确写法
DEPLOY_TOKEN = os.environ.get('DEPLOY_TOKEN', '默认开发用值')
```

- 代码泄露时 Token 不会暴露
- 生产和开发环境可用不同的 Token
- 如果环境变量未设置，自动降级到默认值，不影响开发调试

---

## 三、新增模块开发模板

### 3.1 创建后端路由文件 `routes/new_module.py`

```python
# -*- coding: utf-8 -*-
"""新模块说明"""
import os, json

from flask import Blueprint, jsonify, request

from .utils import TZ, make_logger, make_db

bp = Blueprint('new_module', __name__)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# ========== 数据库 ==========
DB_DIR = os.path.join(BASE_DIR, '新模块目录')
DB_FILE = os.path.join(DB_DIR, 'data.db')
_get_db = make_db(DB_FILE)

def init_db():
    """初始化数据库表"""
    os.makedirs(DB_DIR, exist_ok=True)
    conn = _get_db()
    conn.execute('''CREATE TABLE IF NOT EXISTS xxx (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        ...,
        updated_at TEXT DEFAULT (datetime('now','localtime'))
    )''')
    conn.commit()
    conn.close()

# ========== 日志 ==========
_log = make_logger(os.path.join(DB_DIR, 'app.log'))

# ========== API 路由 ==========

@bp.route('/api/new_module/data', methods=['GET'])
def get_data():
    """读取数据"""
    try:
        conn = _get_db()
        rows = conn.execute('SELECT * FROM xxx ORDER BY id').fetchall()
        conn.close()
        return jsonify({'success': True, 'data': [dict(r) for r in rows]})
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 500


@bp.route('/api/new_module/data', methods=['POST'])
def save_data():
    """保存数据"""
    try:
        data = request.get_json(silent=True) or {}
        # 参数校验
        if not data.get('name'):
            return jsonify({'success': False, 'error': '缺少必要参数'}), 400
        # 业务处理
        conn = _get_db()
        conn.execute('INSERT INTO xxx (name, ...) VALUES (?, ...)', (data['name'], ...))
        conn.commit()
        conn.close()
        _log(f'新增记录: {data["name"]}')
        return jsonify({'success': True})
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 500
```

### 3.2 在 `app.py` 中注册

在 `MODULES` 列表中按顺序添加：

```python
MODULES = [
    ('routes.renqing', 'renqing'),
    ...
    ('routes.new_module', 'new_module'),   # 新模块
]
```

在 `_SAFE_INITS` 中添加：

```python
_SAFE_INITS = [
    ...
    ('new_module', init_new_module_db),
]
```

**注意**：不要在 `app.py` 中直接 `from routes.new_module import bp`，统一走容错导入机制。

### 3.3 添加前端路由（`app.py`）

```python
@app.route('/new_module')
@app.route('/new_module/')
def new_module_index():
    return send_from_directory('新模块目录', 'index.html')
```

### 3.4 如需免登录访问

在 `app.py` 的 `check_auth()` 中添加：

```python
PUBLIC_PREFIXES = ('/static', '/countdown', '/api/countdown', 
                   '/api/pa/', '/api/status', '/new_module', '/api/new_module')
```

### 3.5 新增模块同步清单

**每次新增模块，以下位置必须同步更新，严禁遗漏任何一处：**

| 序号 | 文件 | 位置 | 更新内容 |
|------|------|------|----------|
| 1 | `app.py` | MODULES 列表 | 注册 Blueprint |
| 2 | `app.py` | _SAFE_INITS 列表 | 注册 init_db |
| 3 | `app.py` | 前端路由 | 添加 `@app.route('/new_module')` 前端页面路由 |
| 4 | `app.py` | 前端路由（免登录） | 如免登录，在 `PUBLIC_PREFIXES` 中添加 |
| 5 | `部署/index.html` | app-grid 区 | 添加模块入口卡片 |
| 6 | `部署/index.html` | btm-links 区 | 添加底部链接 |
| 7 | `routes/utils.py` | FOLDER_MAP | 添加英文到中文目录映射 |
| 8 | `routes/deploy.py` | KNOWN_DB_DIRS | 添加中文目录名（概览页数据库扫描） |
| 9 | `routes/deploy.py` | allowed_prefixes | 添加 `'新模块目录/'`（restore-db 白名单） |
| 10 | `routes/backup.py` | DB_PATHS | 添加 `('data.db', 目录)`) 备份目标 |
| 11 | `routes/backup.py` | restore_backup > db_targets | 添加同上的恢复目标 |
| 12 | `开发规范.md` | 一、项目架构总览 | 添加路由文件和中文目录 |
| 13 | `routes/utils.py` | **KNOWN_MODULE_DIRS** | **添加中文目录名**（遗漏会被自动清理整体删除，见坑点 7.41） |
| 14 | `导航/index.html` | app-grid 区 | 添加模块入口卡片（当前主入口页） |

**检查方法**：搜索整个项目代码，用新模块的英文名和中文目录名分别搜索，确保每个上述位置都出现了。

---

### 4.1 强制使用 `make_db()` 工厂

**严禁在任何模块中直接 `sqlite3.connect()`。** 必须统一通过 `utils.make_db()` 创建连接工厂。

```python
from .utils import make_db
_get_db = make_db(db_file_path)
```

**禁止的写法**（以下两种都会导致问题）：

```python
# 禁止：直接导入 sqlite3
import sqlite3
conn = sqlite3.connect(path)

# 禁止：手动设置 PRAGMA
conn = sqlite3.connect(path)
conn.execute('PRAGMA journal_mode=WAL')
conn.execute('PRAGMA busy_timeout=5000')
```

**原因**：直接 `sqlite3.connect()` 缺少 WAL 日志模式（并发读写保护）和 busy_timeout（锁等待），在高并发或异常情况下会导致 `database is locked` 错误。本项目曾因 gpa.py 和 hsgrades.py 绕过 make_db() 而埋下隐患，已在 2026-07-19 审查中修复。

`make_db()` 自动设置：
- `PRAGMA journal_mode=WAL` -- 写前日志，支持并发读
- `PRAGMA synchronous=NORMAL` -- 平衡安全和性能
- `PRAGMA busy_timeout=5000` -- 5 秒忙等待超时

### 4.2 数据库位置

- 存放在模块对应的中文目录下（如 `人情/gifts.db`）
- 数据库文件不纳入 git（`.gitignore` 中 `*.db`）
- 如有种子数据需求，使用 `gen_seed.py` 生成 base64 编码的 `seed_data.py`

### 4.3 表设计规范

- **必须包含**：`id INTEGER PRIMARY KEY AUTOINCREMENT`
- **建议包含**：`created_at TEXT DEFAULT (datetime('now','localtime'))`、`updated_at`
- **JSON 存储**：简单配置类数据可用单行 JSON 存储，减少表结构变更
- **Schema 版本**：如需表结构迁移，添加 `schema_version` 表记录版本号

### 4.4 数据库操作

```python
# 查询
conn = _get_db()
rows = conn.execute('SELECT * FROM xxx WHERE ...', (param,)).fetchall()
conn.close()

# 写入（自动事务）
conn = _get_db()
conn.execute('INSERT INTO xxx (...) VALUES (?, ?, ...)', (a, b, c))
conn.commit()
conn.close()

# 更新
conn = _get_db()
conn.execute('UPDATE xxx SET field=? WHERE id=?', (value, eid))
conn.commit()
conn.close()
```

### 4.5 SQL 拼接安全规则

**用户输入值必须用参数化查询（`?` 占位符），禁止 f-string 拼接用户数据。**

| 场景 | 做法 | 示例 |
|---|---|---|
| 用户输入值 | 必须 `?` 占位符 | `conn.execute('WHERE name=?', (user_input,))` |
| 内部构造的表名 | 允许 f-string | `f'SELECT * FROM [{table_name}]'`（表名来自 DB 元数据） |
| 内部构造的子句 | 允许 f-string | `w = 'WHERE ...' if condition else ''`（子句逻辑在代码内） |
| LIKE 查询 | 参数化 + 拼接 | `conn.execute('WHERE name LIKE ?', (f'%{kw}%',))` |

**关键原则**：只要数据来源不是用户直接输入，而是代码内部控制的值（表名从 `sqlite_master` 查询、子句由 if/else 决定），则 f-string 是安全的。如有疑问，一律用 `?` 占位符。

```python
# 正确：用户输入用占位符
conn.execute('SELECT * FROM records WHERE name LIKE ?', (f'%{keyword}%',))

# 正确：表名来自 DB 元数据，使用 f-string
for (table_name,) in conn.execute("SELECT name FROM sqlite_master WHERE type='table'"):
    conn.execute(f'SELECT COUNT(*) FROM [{table_name}]')

# 错误：用户输入直接拼进 SQL（SQL 注入风险！）
conn.execute(f"SELECT * FROM records WHERE name = '{user_name}'")
```

---

## 五、前端规范

### 5.1 基础结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>模块名</title>
    <style>
        /* 全部 CSS 写在这里，不引入外部样式 */
        :root {
            --bg: #f5f7fa;
            --card-bg: #ffffff;
            --text: #333333;
            --primary: #667eea;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        /* ... */
    </style>
</head>
<body>
    <!-- HTML 内容 -->
    
    <script>
        // 全部 JS 写在这里，不引入外部脚本（Chart.js 除外）
        
        // API 调用基础函数
        async function api(url, options = {}) {
            const res = await fetch(url, {
                headers: { 'Content-Type': 'application/json', ...options.headers },
                ...options
            });
            return await res.json();
        }
        
        // 业务逻辑
    </script>
</body>
</html>
```

### 5.2 设计原则

- **纯文字版本**：不使用图标/emoji 符号，状态和操作用文字表达
- **配色方案**（2026-07-27 统一为浅色常规配色）：

| 用途 | 色值 | 说明 |
|------|------|------|
| 页面底色 | `#f0f2f5` | 全局背景，替代旧深色 `#0f172a` |
| 卡片/面板 | `#ffffff` | 白色卡片，搭配细边框和轻阴影 |
| 主文字 | `#1e293b` | 标题、正文 |
| 次文字 | `#64748b` / `#94a3b8` | 辅助说明、标签 |
| 主色调 | `#4f46e5` (indigo) | 导航、按钮、链接、选中态 |
| 边框 | `#e2e8f0` / `#d1d5db` | 卡片边框、输入框边框 |
| 阴影 | `rgba(0,0,0,.04-0.06)` | 轻量阴影，替代旧版重阴影 |
| 成功/收入 | `#059669` / `#10b981` | 绿色系 |
| 警告 | `#d97706` | 橙色系 |
| 危险/支出 | `#dc2626` / `#ef4444` | 红色系 |
| 输入背景 | `#ffffff` | 白色背景+浅灰边框 |
| 页头/导航栏 | `#ffffff` | 白色背景+底部分割线 |
| manifest theme-color | `#f8fafc` | PWA 状态栏颜色 |

- **SPA 单页**：前端状态用 JS 变量管理，不依赖 URL hash

### 5.3 移动端适配（强制）

本项目多数场景为手机端使用，所有前端页面必须适配移动端。

**核心原则：双模式设计 —— 移动端 App 效果 + PC 端大屏显示**

| 模式 | 断点 | 布局 | 导航 | 内容宽度 |
|------|------|------|------|----------|
| 移动端 App | ≤768px | 全屏沉浸式 | 底部固定导航栏 | 100vw，padding 12-16px |
| PC 端大屏 | >768px | 传统桌面布局 | 顶部/侧边导航栏 | max-width 限制，居中显示 |

通过 `@media (max-width: 768px)` 和 `@media (min-width: 769px)` 媒体查询实现两种模式切换。**统一断点为 768px**，不允许使用其他断点值（如 640px、480px）。

**移动端 App 效果要求（≤768px）**：
- **全屏沉浸布局**：`html, body { height: 100%; overflow: hidden }`，body 使用 `display: flex; flex-direction: column` 占满视口
- **底部固定导航**：使用 `position: fixed; bottom: 0` 的底部标签栏切换页面，**仅在移动端显示**（`@media (min-width: 769px) { .bottom-nav { display: none; } }`）
- **滑动操作**：支持滑动删除等手势交互
- **下拉刷新**：支持下拉刷新数据
- **弹窗全屏**：编辑/新增弹窗在移动端全屏显示

**PC 端大屏要求（>768px）**：
- **内容居中**：主要内容区域设置 `max-width`（建议 900-1400px），`margin: 0 auto` 居中
- **顶部导航**：使用页面顶部的导航标签/按钮，**隐藏底部导航栏**
- **充分利用宽度**：统计面板、表格等使用多列网格或自适应 `auto-fit` 布局
- **侧边栏可选**：管理类页面可使用侧边栏布局（如部署页）

**布局**：
- 使用单列流体布局，避免多列并排（统计图表除外）
- 页面最大宽度不超过 100vw，禁止水平滚动
- 卡片/表单容器使用 `width: 100%` + `max-width` 限制，左右留适当 padding（建议 12px-16px）

**触控**：
- 所有可点击元素（按钮、链接、选择器）最小触控区域 44x44px
- 相邻可点击元素间距至少 8px，防止误触
- 表单输入框高度不小于 40px

**字体**：
- 正文字号不小于 14px（iOS 会自动缩放小于 16px 的输入框），输入框字号建议 16px
- 数字/金额展示字号适当加大（18px-22px），便于快速扫读

**Viewport**：
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
```

**安全区域**（刘海屏/底部指示条）：
```css
body {
    padding-top: env(safe-area-inset-top);
    padding-bottom: env(safe-area-inset-bottom);
    padding-left: env(safe-area-inset-left);
    padding-right: env(safe-area-inset-right);
}
```

**表格/列表**：
- 表格列数超过 3 列时，考虑卡片式布局替代表格
- 必须用表格时，设置 `overflow-x: auto` 允许横向滚动
- 长文本使用 `overflow: hidden; text-overflow: ellipsis; white-space: nowrap`

**弹窗/对话框**：
- 弹窗宽度使用 `min(90vw, 500px)`，确保小屏不溢出
- 弹窗关闭按钮放在显眼位置（右上角或底部），触控区域足够大

**测试要求**：
- 开发完成后用 Chrome DevTools 模拟 iPhone SE（375px）、iPhone 12（390px）、iPad（768px）三种宽度测试
- 检查所有表单输入、按钮点击、下拉选择在触控模式下的可用性

### 5.4 PWA 支持（可选）

如需离线使用，添加：
- `manifest.json` 或动态生成 Blob Manifest
- Service Worker (`sw.js`)，缓存静态资源和 API 响应
- 图标文件：`icon-192.png`、`icon-512.png`

### 5.5 全局组件统一样式标准

> 2026-07-28 全局风格统一整改后确立。所有页面必须遵循。

**CSS 变量名规范**：

```css
:root {
    --card-bg: #ffffff;          /* 注意：统一使用 --card-bg，不是 --bg-card */
    --text: #1e293b;
    --text-secondary: #64748b;
    --shadow: 0 2px 8px rgba(0,0,0,.06);
    --danger: #dc2626;
}
```

**全局字体栈**（所有页面 body 必须设置）：

```css
body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
}
```

**页面头部（Header）**：

```css
header {
    padding: 12px 16px;
    padding-top: calc(12px + env(safe-area-inset-top));
    font-size: 17px;
    font-weight: 600;
    border-bottom: 1px solid #e2e8f0;
}
```

**按钮**：

```css
button, .btn {
    border-radius: 10px;
    padding: 10px 16px;
    font-weight: 600;
    min-height: 44px;
}
```

**输入框**：

```css
input, select, textarea {
    border-radius: 10px;
    padding: 10px 12px;
    min-height: 44px;
}
```

**卡片**：

```css
.card { padding: 16px; border-radius: 12px; }          /* 桌面端 */
@media (max-width: 768px) {
    .card { border-radius: 10px; }                       /* 移动端 */
}
```

### 5.6 返回箭头与页面头部规范

所有子页面头部的返回箭头（导航回首页 `/nav`）必须遵循统一样式，**严禁各页面自行定义**。

**统一样式**：

```css
.back-arrow {
    color: #64748b;
    font-size: 20px;
    font-weight: 400;
    min-width: 44px;
    min-height: 44px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
}
```

**布局模式：必须使用 flex 布局，禁止 absolute 定位**：

```html
<!-- 正确：箭头与标题用 flex 包裹 -->
<header>
  <div style="display:flex;align-items:center;gap:8px">
    <a class="back-arrow" href="/nav">&lt;</a>
    <span>页面标题</span>
  </div>
</header>

<!-- 错误：absolute 定位在不同设备上位置不可控，已被废弃 -->
<header style="position:relative">
  <a style="position:absolute;left:6px" href="/nav">&lt;</a>
  页面标题
</header>
```

**为什么不用 absolute**：刘海屏/岛屏手机 safe-area 不一致，absolute 定位容易偏移或被状态栏遮挡。flex 自动垂直居中，不受 safe-area 影响。

### 5.7 移动端底部导航栏规范

底部 TabBar 导航栏（移动端 ≤768px 时显示）在平台内各页面必须统一：

```css
.tab-item {
    font-size: 14px;
    font-weight: 700;
    /* 激活态 */
}
.tab-item.active {
    font-weight: 800;
}
```

**强制要求**：
- **纯文字，禁止使用任何符号/emoji/图标**作为导航项前缀（如 `~概览`、`*成绩` 等均为不规范写法）
- 导航项文字直接作为内容，不包裹在多余 span 中
- 各页面导航项文字保持 2-3 字简洁

正确示例：
```html
<div class="tab-bar">
  <div class="tab-item active">概览</div>
  <div class="tab-item">成绩</div>
</div>
```

### 5.8 Favicon/LOGO 浏览器标识规范

**每个 HTML 页面必须设置 favicon**，确保浏览器标签页和书签栏展示 LOGO。

导航主页使用项目 SVG 图标文件：

```html
<link rel="icon" type="image/svg+xml" href="/nav/icon-192.svg">
```

子页面可用各自模块的图标文件：

```html
<link rel="icon" type="image/svg+xml" href="/模块名/icon-512.svg">
```

纯后端渲染页面（登录页、设置页等没有静态文件目录的页面）使用内联 data URI：

```html
<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,...">
```

**theme-color 配合**：`<meta name="theme-color">` 应与主色调一致（如导航页 `#4f46e5`），使浏览器顶栏与 LOGO 配色协调。

### 5.9 等宽字体/长文本防溢出

日志框、代码块、表格单元格等内容区域如果使用了等宽字体（`font-family: monospace`），移动端长文本可能撑破容器导致页面弹回大尺寸布局。

**强制要求**：等宽字体元素必须添加溢出保护：

```css
.log-box, code, pre {
    word-break: break-all;
    overflow-wrap: break-word;
    max-width: 100%;
}
```

同时页面主容器添加：

```css
.container {
    max-width: 100vw;
    overflow-x: hidden;
}
```

**教训**：PA 续期页日志框曾因 monospace + 长行导致页面在 768px 断点处反复弹跳，本质是等宽字体每个字符宽度固定，不自动换行。

### 5.10 全平台 UI 一致性铁律

所有模块页面通过导航页（`/nav`）访问，对用户而言是一个统一的 App，而非零散的独立页面。因此所有模块必须保持高度一致的 UI 体验。

**Header 强制标准模板**：

Body 必须设置 `padding-top` 兜底（`env()` 在某些 iOS 版本可能返回 0，仅靠 header 的 padding-top 不够可靠）：

```css
body {
    padding-top: env(safe-area-inset-top);
    padding-left: env(safe-area-inset-left);
    padding-right: env(safe-area-inset-right);
    padding-bottom: calc(16px + env(safe-area-inset-bottom));
}
/* 兜底遮罩：确保刘海区域始终显示 header 背景色 */
body::before {
    content: '';
    position: fixed; top: 0; left: 0; right: 0;
    height: env(safe-area-inset-top);
    background: #ffffff;   /* 与 header 背景色一致 */
    z-index: 101;
    pointer-events: none;
}
```

Header（所有模块 header 必须完全一致）：
```css
header {
    background: #ffffff;
    border-bottom: 1px solid #e2e8f0;
    padding: 12px 16px;
    position: sticky;
    top: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    /* 禁止 box-shadow */
    /* 禁止在 header 的 padding 中使用 env(safe-area-inset-*) — 由 body 统一处理 */
}
```

**为什么要在 body 而非 header 上处理 safe-area？**
- `env(safe-area-inset-top)` 在部分 iOS 版本或非独立模式下可能返回 0，仅靠 header `padding-top: calc(12px + env(...))` 可能失效
- body `padding-top` + `::before` 伪元素提供双重保障：body padding 推下内容，`::before` 在视觉上填充刘海区域
- 即使 `env()` 返回 0，`::before` 高度为 0 也不会造成副作用

注：对于不包含长 nav 标签栏的简单模块（如绩点、成绩），header 可保留 `padding-top: calc(12px + env(safe-area-inset-top))` 但 body 仍需加 `padding-top` 兜底。

**返回箭头强制标准模板**：

所有模块返回箭头必须是 `<a href="/nav">←</a>`，内联样式完全一致：
```html
<a href="/nav" title="返回导航"
   style="color:#64748b;text-decoration:none;font-size:20px;font-weight:400;
          line-height:1;display:inline-flex;align-items:center;
          justify-content:center;min-width:44px;min-height:44px;
          border-radius:8px"
   onclick="if(document.referrer.includes('/nav')){history.back();return false}">←</a>
```

**禁止事项**：
- 禁止 header 使用 `box-shadow`
- 禁止 header 使用非标准 padding（如 `0 20px`、缺少 safe-area）
- 禁止返回箭头使用 `<` / `&lt;` / `<button>` / 自定义 class（必须是内联 `<a>` + `←`）
- 禁止 header 使用 `<div class="hdr">` 替代 `<header>` 标签
- 禁止 header 标题字体大小/粗细不一致

**检查清单**：新增模块或修改现有模块 UI 时，必须逐项检查：
1. header CSS 是否与标准模板一致
2. 返回箭头 HTML 是否与标准模板一致
3. safe-area-inset-top/left/right 是否补齐
4. 是否有多余的 box-shadow
5. 触控目标是否 >= 44px
6. 移动端输入框字号是否 >= 16px
7. 底部导航是否符合 5.7 规范

---

## 六、部署流程规范

### 6.1 部署流水线

```
本地修改代码 → git commit → git push → 触发部署 API → 服务器 git pull → 自动重载
```

### 6.2 自动推送部署原则（强制）

**核心铁律：代码修改后自动 git push + 部署，中间不中断。**

这意味着：
- 每次代码修改完成后，git add/commit/push 和触发部署是绑定的一体化流程
- 两条命令视为一个原子操作，必须依次连续执行完毕，不得拆分
- 中间不得停顿、不得等待用户确认、不得因任何原因跳过
- 如果部署失败应自动重试，不能跳过部署步骤
- 即使是"小改动"、"修个文案"、"改个 CSS" 也不得省略此流程

**执行命令（原子操作）**：
```bash
git add -A && git commit -m "..." && git push origin master:master master:main --force
curl.exe -X POST https://gjx.pythonanywhere.com/api/git-pull?force=1 -H "X-Deploy-Token: ce952b9ded0733ed" -H "Content-Type: application/json"
```

**注意事项**：
- Windows PowerShell 环境下 `curl` 是 `Invoke-WebRequest` 的别名，需使用 `curl.exe` 显式调用
- `git push` 同时推送到 `master` 和 `main` 两个分支，保持远端一致
- 部署 Token 通过 `X-Deploy-Token` Header 传递，不暴露在 URL 中

### 6.3 每次代码修改后的必须步骤

1. **语法检查**：`python -m py_compile 修改的文件.py`
2. **导入测试**：`python -c "from routes.xxx import bp; print('OK')"`
3. **本地运行测试**：`python -c "from app import app; c=app.test_client(); ..."`
4. **自动推送部署**：按 6.2 节执行原子操作，语法检查和部署之间的步骤可酌情合并
5. **验证线上**：`curl https://gjx.pythonanywhere.com/api/xxx/data`

### 6.4 部署死锁的教训

**问题**：部署 API 是 Flask 路由，代码有 bug → 应用崩溃 → 部署 API 不可用 → 无法远程恢复。

**解决方案**（已实施）：
- `deploy` 蓝图在 `app.py` 中最先注册
- 所有路由模块用 `_safe_import()` 容错导入，单个模块出错不影响 deploy 和其他模块
- 如果有模块导入失败，会在启动日志中打印 `[WARNING]`

### 6.5 紧急恢复方案

如果应用完全崩溃（部署 API 不可用）：
1. 登录 PythonAnywhere，打开 Bash Console
2. 进入代码目录：`cd /home/Gjx/toolbox`
3. 拉取最新代码：`git pull`
4. 清除缓存：`find . -name "__pycache__" -type d -exec rm -rf {} +`
5. Web 标签页点击 **Reload**

### 6.6 PythonAnywhere 平台管理

#### 6.6.1 账户信息

| 项目 | 值 |
|------|-----|
| 平台 | PythonAnywhere (pythonanywhere.com) |
| 账户名 | Gjx |
| 应用域名 | gjx.pythonanywhere.com |
| 代码路径 | `/home/Gjx/toolbox` |
| Python 版本 | 3.10+ |

#### 6.6.2 Web App 配置

**通过 PA Web 面板管理**：
- **Source code**: `/home/Gjx/toolbox`
- **Working directory**: `/home/Gjx/toolbox`
- **WSGI configuration file**: 引导到 `app.py` 中的 Flask 应用对象
- **Static files**: 不在 PA 层配置，由 Flask 的 `send_from_directory` 统一处理

#### 6.6.3 部署 API 原理

```
POST /api/git-pull
Header: X-Deploy-Token: ce952b9ded0733ed

服务器执行:
  1. 验证 Token
  2. subprocess.run(["git", "pull"], cwd=app_root)   # 拉取最新代码
  3. 清理 __pycache__
  4. os.execv(sys.executable, ...)                    # 自重启进程
```

**关键点**：
- Token 通过 `os.environ.get('DEPLOY_TOKEN')` 管理，不硬编码在公开仓库
- `os.execv` 会替换当前进程，使 Flask 自动重新加载所有模块
- 部署 API 注册在 `deploy` 蓝图中，该蓝图在 `app.py` 中最先注册

#### 6.6.4 免费账户限制

| 限制项 | 影响 | 对策 |
|--------|------|------|
| 磁盘配额 512MB | 大文件/日志积累可能超出 | 定期清理旧日志 (>7天)；数据库备份仅保留最近 7 份 |
| 无定时任务 | 不能使用 cron | 需要时手动触发或外部调用 API |
| 每日请求限制 | 高并发可能触发限制 | 前端使用 SPA 减少页面请求；静态资源缓存 |
| 进程超时 | 长时间请求可能被 kill | 耗时操作异步化；数据库操作保持简单 |
| 外出请求白名单 | 只能访问白名单域名 | API 调用目标域名需在 PA 白名单中 |

#### 6.6.5 日常运维命令

```bash
# 登录 PA Bash Console 后常用的命令：

# 查看应用日志
tail -f /var/log/gjx.pythonanywhere.com.server.log
tail -f /var/log/gjx.pythonanywhere.com.error.log

# 手动拉取代码
cd /home/Gjx/toolbox && git pull

# 手动重载应用（等效于 Web 面板点 Reload）
touch /var/www/gjx_pythonanywhere_com_wsgi.py

# 查看数据库
sqlite3 /home/Gjx/toolbox/data/database.db ".tables"

# 磁盘使用
du -sh /home/Gjx/toolbox/*
```

---

## 七、坑点清单（避雷指南）

### 7.1 缩进错误（已踩坑）
**现象**：XX模块报 500，排查发现缩进是 8 个空格而非 4 个  
**根因**：代码编辑时误按 Tab 或粘贴时缩进错乱  
**预防**：提交前用 `py_compile` 检查每个文件语法；开启编辑器空白字符显示

### 7.2 服务器路径不一致（已踩坑）
**现象**：本地 `~/tools` 操作正常，服务器仍 500  
**根因**：服务器实际代码路径是 `/home/Gjx/toolbox`  
**预防**：永远通过部署 API 触发更新，不要假设服务器路径

### 7.3 中文目录名跨平台（已踩坑）
**现象**：Windows 开发正常，Linux 部署报路径错误  
**根因**：中文目录名编码在不同 OS 下处理不一致  
**对策**：统一使用 `os.path.join()` 拼接路径；所有 `.py` 文件声明 `utf-8` 编码

### 7.4 可选依赖崩溃（已踩坑）
**现象**：倒计时模块因 `zhdate` 导入失败而崩溃  
**根因**：生产环境未安装农历库，但代码在模块顶层 `import` 导致 ImportError  
**对策**：可选依赖使用 try/except 懒加载，在函数内部检查可用性。参考 `countdown.py` 中 `HAS_LUNAR` 的处理方式

### 7.5 数据库连接不关闭
**现象**：SQLite 报 `database is locked`  
**根因**：连接使用后未关闭，或异常路径未关连接  
**对策**：每次请求创建新连接，用完立即 `conn.close()`；避免在循环中反复开关连接

### 7.6 部署 API 依赖应用（已踩坑）
**现象**：代码 bug 导致全站 500，部署 API 也无法使用  
**根因**：部署 API 是 Flask 蓝图的一部分，需要应用成功启动  
**对策**：详见第六章「部署死锁的教训」

### 7.7 路径分隔符
**现象**：`routes.randeng\utils` 或 `人情/index.html`  
**根因**：混用 `\` 和 `/` 分隔符  
**对策**：全部使用 `os.path.join()` 和 `os.sep`；URL 路径只用 `/`

### 7.8 PA 磁盘配额
**现象**：文件写入失败  
**根因**：PythonAnywhere 免费账户有严格的空间限制  
**对策**：定期清理日志；数据库备份保留最近 7 个；定期清理 `__pycache__`

### 7.9 Token 硬编码（已修复）
**现象**：`DEPLOY_TOKEN` 写死在代码中  
**风险**：代码推送至公开仓库则 Token 泄露，任何人可触发部署  
**改善**：迁移到 `os.environ.get('DEPLOY_TOKEN', '默认值')`，环境变量优先

### 7.10 绕过 make_db() 直接 sqlite3.connect()（已踩坑/已修复）
**现象**：gpa.py、hsgrades.py 直接 `import sqlite3` + `sqlite3.connect()`  
**根因**：早期模块未统一规范，各自实现数据库连接  
**后果**：缺少 WAL 日志模式和 busy_timeout，并发场景下可能触发锁错误  
**对策**：强制所有模块使用 `make_db()` 工厂，已在 2026-07-19 审查中统一修复

### 7.11 f-string 拼 SQL 的误判
**现象**：`f'SELECT COUNT(*) FROM [{t}]'` 看起来像 SQL 注入  
**实际**：`t` 来自 `sqlite_master` 系统表（DB 元数据），不是用户输入  
**原则**：审查 f-string SQL 时，关键是判断拼接值来源——内部可控 = 安全，用户输入 = 注入风险

### 7.12 JS 中事件状态列表查找遗漏（已踩坑）
**现象**：人情模块中，归档/未归档事件的数据查找逻辑只在 `events`（已归档列表）中 `find`，未引入 `isUnarchivedMode` 判断  
**根因**：`events` 变量存储的是已归档事件子集，未归档事件存在 `unarchivedEvts` 变量中。代码中 4 处 `events.find(e=>e.id===selEvtId)` 未根据 `isUnarchivedMode` 在正确列表中查找  
**影响**：查看未归档事件时，翻页保存拿不到正确的 `page_size` 和 `event_date`；事件详情面板的归档按钮状态不更新  
**修复**：全部改为 `(isUnarchivedMode ? unarchivedEvts : events).find(e=>e.id===selEvtId)`  
**原则**：前端维护多个并行数据子集（如归档/未归档）时，所有查找操作必须根据当前模式选择正确的列表

### 7.13 Response 对象属性混用 .ok 和 .success（已踩坑/已修复）
**现象**：savePage 中用 `r.value.success` 判断 fetch 结果，导致即使后端返回错误（如 400），前端也静默报告"保存成功"  
**根因**：`Promise.allSettled` 返回的每个 `r.value` 是 fetch 的 Response 对象，该对象只有 `.ok` 属性，没有 `.success` 属性。后端 JSON 中的 `success` 字段需要先 `await r.value.json()` 才能访问  
**错误写法**：`results.filter(r=>r.status==='fulfilled'&&r.value.success)` — 永远是 0 条失败  
**正确写法**：`results.filter(r=>r.status==='fulfilled'&&r.value.ok)` — 用 Response 原生属性  
**原则**：fetch Response 对象用 `.ok` 判断 HTTP 状态，后端业务逻辑的 `success` 需先解析 JSON

### 7.14 HTML 行内事件处理器括号不匹配（已踩坑/已修复）
**现象**：number 输入框改动后预览不更新，浏览器控制台报 `Uncaught SyntaxError: missing ) after argument list`  
**根因**：`oninput="updateState('key', parseFloat(this.value)||1">` 中 `updateState(` 调用缺少右括号，HTML 解析后 JavaScript 立即执行报错，整行事件失效  
**修复**：补齐为 `oninput="updateState('key', parseFloat(this.value)||1)"`  
**原则**：行内事件属性本质是 JavaScript 代码片段，必须保持完整语法；使用 IDE/linters 检查 HTML 中的 inline JavaScript；复杂逻辑优先写到 `<script>` 中通过 `addEventListener` 绑定，减少括号嵌套错误

### 7.15 密码编码格式迁移注意事项
**现象**：升级到 PBKDF2 + HMAC 密码编码后（`v2:` 前缀），`reload_webapp.py` 的 fallback `decode_pw` 只支持旧 XOR 格式，若 import utils 失败会导致密码解码失败  
**修复**：`reload_webapp.py` 的 `load_credentials()` 统一使用 `utils.decode_pw()`（自动识别 v2:/ENC:/base64 三种格式）；fallback 仅作应急备用，新格式密码在 fallback 路径下会解析失败但不会导致崩溃  
**原则**：多文件间共享的编解码逻辑必须由单一源文件提供（`utils.py`），严禁复制代码；格式兼容性通过前缀标记（`v2:`, `ENC:`）自动检测

### 7.16 SQLite CHECK 约束迁移死胡同（已踩坑/已修复）
**现象**：服务器启动时 `[init_accounting_db ERROR] CHECK constraint failed`，uWSGI 进程启动后几分钟内静默崩溃（ps 无进程、无错误日志）。客户端访问返回 502/504 或超时无响应  
**根因**：categories 表的 CHECK 约束从旧版 `type IN ('收入','支出','他欠我','我欠他')` 变更为新版 `type IN ('付给','收到','应收')`。迁移代码按以下错误顺序执行：(a) 先尝试用新 type 值插入旧表 → 旧 CHECK 拒绝；(b) 再尝试重建表并拷贝数据 → 旧数据中的旧 type 值被新 CHECK 拒绝。两边的 CHECK 约束形成「死胡同」，两条路径全部失败  
**后果**：
- 数据库迁移失败，categories 表保留旧结构 + 部分旧数据
- 运行时 `get_categories` 用 `result[r['type']]` 直接下标访问，`r['type']` 为 `'他欠我'` 等旧值不在 `result` 的 key 中 → KeyError
- KeyError 未被上层捕获，导致 uWSGI worker 崩溃，进程静默退出
**修复方案**：
1. 迁移开始时先检测旧 CHECK 是否存在（检查 schema SQL 字符串中是否包含旧 type 值）
2. 若存在旧约束：创建**无 CHECK 约束的临时表** → 全量拷贝旧数据 → 删除旧表 → 创建新 CHECK 表 → 用 `OLD_TO_NEW` 映射转换 type 值后插入 → 删除临时表
3. `get_categories` 等查询函数使用 `if t not in result: result[t] = []` 防御性编程
**核心原则**：
- **SQLite 不支持 ALTER TABLE DROP CONSTRAINT**，CHECK 约束变更必须走「无约束临时表」中转
- **迁移要在 CREATE TABLE IF NOT EXISTS 之前判断**，因为 `IF NOT EXISTS` 不会改变已存在的旧表结构
- **init_db 的 try/except 不能掩盖数据不一致** — 初始化"完成"不代表数据正确，运行时查询仍会因脏数据崩溃
- **从数据库读取的值用作 dict key 时**，必须用 `.get()` 或 `in` 检查，不能假定值一定在预期范围内

### 7.17 init_db 缺失 try/except 导致静默初始化失败（已排查/已修复）
**现象**：排查宕机隐患时发现 `routes/pa.py` 的 `init_db()` 函数是唯一一个完全没有 try/except 包裹的初始化函数。虽然后续调用链在 `app.py` 中有捕获，但 pa 模块本身就是负责服务器定时续期的关键模块，初始化失败会导致续期功能完全不可用  
**根因**：逻辑代码裸露在函数顶层，`_get_db()` 可能因文件权限/磁盘满抛出 `sqlite3.OperationalError`，密码迁移逻辑也可能因 `decode_pw`/`encode_pw` 抛异常，全部直接穿透  
**修复**：将函数体移到 `_init_db_impl()`，`init_db()` 外层包裹 `try/except` 打印日志（与其他模块的 init_db 保持一致）  
**原则**：**所有 init_db 函数必须有 try/except 包裹**，异常只能打日志不能阻断启动，但必须确保调用方知道初始化失败（当前 `app.py` 的 `_SAFE_INITS` 设计已具备此能力）

### 7.18 隐患排查方法论总结
经本次全面排查，识别以下**高风险模式**作为以后开发的必查项：
- **CHECK 约束迁移**：所有带 CHECK 的表结构变更必须走「无约束临时表中转」，严禁直接 `INSERT INTO new_table SELECT * FROM old_table`
- **init_db 无 try/except**：每个模块的 init_db 必须有异常兜底
- **迁移提前 return**：init_db 内 `conn.commit()` 后的 return 必须不跳过后续统一收尾逻辑（如再查 Excel 可放迁移块之外）
- **dict 下标访问**：从数据库读取的值当作 dict key 使用时，必须 `in` 检查或 `.get()` 防御

以下模式**不会导致进程崩溃**（Flask 框架兜底），但仍属不良实践：
- 路由函数 `try/finally` 无 `except`（Flask 500 handler 会捕获）
- 数据库连接未显式 close（进程退出时自动回收）
- 线程不安全的全局字典操作（GIL 保护单次操作）

### 7.19 PA 服务重载真空窗口问题（已优化/待观察）
**现象**：代码部署后或手动点击 PA Web 重载按钮后，服务会有一段不可用时间（30秒~2分钟），期间访问返回 502/504
**根因**：
- PA 的 reload 流程：SIGTERM 旧 uWSGI → 旧进程退出 → 新 uWSGI 启动 → 初始化数据库 → 接受请求
- `reload_webapp.py` 的传统网页重载方式需要 4 次 HTTP + CSRF + BeautifulSoup 解析，最坏耗时 75 秒
- API 重载方式快但 PA API 偶发不可用时会直接失败，无重试机制 → 跳到网页方式 → 更慢
**优化措施**：
1. API 重载增加 3 次重试 + 3/6/9 秒退避策略（PA API 偶尔 502/503 属正常波动）
2. 重载后主动轮询验证（30 秒内检查 `/api/status` 是否恢复 200，确认新进程已就绪）
3. 日志完整记录每次重载尝试的结果和耗时
**注意**：真空窗口无法完全消除（PA 基础设施必须杀旧启新），但通过 API 优先 + 重试机制可压缩到约 20~30 秒

### 7.20 状态查询多次扫描同一磁盘的重复 I/O 问题（已修复）
**现象**：`_build_server_status()` 先调用 `_dir_size(BASE_DIR)` 扫描全项目，再对 8 个子目录各自调用 `_dir_size()`，同一批文件被 `stat()` 至少 2 次。PA 低配 I/O 下每次状态刷新阻塞明显。
**根因**：为了实现「存储分布」功能，在后者的逐目录统计中新增了独立的 `_dir_size` 调用，未复用已有的全项目扫描结果。
**修复**：改为单次 `os.scandir` 遍历 BASE_DIR，途中同时收集总量和各子目录大小，`_dir_size` 仅调用 N 次（N=一级子目录数，每目录遍历一次），每个文件只 stat 一次。
**教训**：任何时候做目录/文件扫描，先规划好「我需要哪些数据」，尽量一次遍历收集所有数据，不要写多个独立的遍历逻辑。

### 7.21 SQLite 迁移检测应使用 PRAGMA user_version 标记（已修复）
**现象**：`accounting.py` 的 `init_db()` 每次启动都解析 `sqlite_master` 的 SQL 文本检查迁移状态（正则匹配旧 CHECK 约束），即使迁移早在数月前已完成。
**根因**：没有迁移完成标记，每次只能通过 SQL 文本特征判断是否已迁移。虽单次遍历很快（微秒级），但每条 SQL 文本都是字符串解析，累积为不必要的启动开销。
**修复**：在 `init_db()` 末尾添加 `PRAGMA user_version = 2`，下次启动时优先读 `user_version`，版本 ≥ 2 则跳过所有 sqlite_master 解析。
**教训**：任何仅需执行一次的迁移操作，都应该用 `PRAGMA user_version` 或等价的标记机制来跳过已完成的检查，避免每次启动都做无用功。
**最佳实践**：
```python
# 正确做法：启动时先检查版本号
ver = conn.execute("PRAGMA user_version").fetchone()[0]
if ver >= MIGRATION_V1:
    # 跳过已完成的迁移检查
    pass
# ... 执行新迁移 ...
conn.execute("PRAGMA user_version = 2")  # 标记完成
conn.commit()
```

### 7.22 预构建缓存延迟应匹配 PA worker 生命周期（已修复）
**现象**：`prebuild_status()` 的延迟设为 30 秒，但 PA 免费版 worker 有时在 30 秒内已被平台重启，导致缓存线程被 kill 而从未真正执行。用户首次访问 /api/status 时触发同步构建，体验差。
**修复**：延迟从 30 秒缩短到 5 秒（进程启动 5 秒内 I/O 基本完成），确保缓存大概率在 worker 存活期内建成。
**教训**：PA 免费版 worker 生命周期短且不固定（数秒到数分钟），任何延迟初始化的设计都应使用尽可能短的等待时间。

### 7.23 PA 启动阶段禁止同步执行非关键初始化（已修复）
**现象**：PA 重载后报 "Your webapp took a long time to reload"，服务长期不可用。服务端日志是密集的 `OSError: write error`。
**根因**：`app.py` 模块加载阶段同步执行了 6 个数据库初始化 + 3 个后台线程启动 + 缓存预构建，全部堵在 Python 模块 import 阶段。PA 免费版 worker 健康检查超时阈值约 10~15 秒。同步启动耗时超过阈值 → PA 判定 "unable to check" → 服务不可用。
**修复**：
1. 数据库初始化保留同步（表不存在无法处理请求，这是必需的）
2. 后台线程（auto_renew / auto_backup / auto_clean）改为延迟线程：`threading.Thread(target=..., daemon=True).start()`，内部 `time.sleep(3)` 等待 worker 先就绪
3. `prebuild_status` 缓存构建也移入延迟线程
4. `_diag()` 函数的 `print(..., flush=True)` 去掉 `flush=True`（uwsgi 环境下每次 flush 可能阻塞）
**教训**：**PA 启动阶段只做「缺了就没法运行」的事**（数据库表创建）。一切"有了更好、没有也能用"的初始化（后台线程、缓存预热）全部延迟执行，绝对不能堵在 worker 上线路径上。
**适用规则**：以后新增任何模块的 init_db 之外的初始化逻辑，一律用延迟线程模式：
```python
def _lazy_init():
    import time
    time.sleep(3)  # 等 worker 就绪
    # ... 非关键初始化 ...

threading.Thread(target=_lazy_init, daemon=True).start()
```

### 7.24 OSError: write error 不要触发完整错误日志链（已修复）
**现象**：PA 服务器日志中大量 `OSError: write error`，每次伴随 traceback 写入 `500_error.log`，形成日志风暴。
**根因**：客户端断开（超时/关闭标签页/网络问题）时 Flask 尝试写响应失败 → `OSError` → 500 处理器 → `traceback.format_exc()` + `app.logger.error()` + 写 `500_error.log` → 如果再失败 → 循环。
**修复**：添加 `@app.errorhandler(OSError)` 拦截器，识别 `write error` / `BrokenPipeError` / `ConnectionResetError`，静默返回空响应（客户端已断开，无需响应）。
**教训**：客户端断连类错误不属于服务端 bug，不应写 error log 也不应 traceback。单独拦截、静默处理。

### 7.25 后台服务管理器 - 按需启停 **[已取消]**

曾尝试统一管理后台服务启停（`routes/service_manager.py`），后因复杂度收益不成比例而取消。

### 7.26 `make_db()` 返回工厂函数，不是连接对象

**现象**：数据库操作报 `AttributeError: 'function' object has no attribute 'execute'`。
**根因**：`make_db(db_file)` 返回的是 `_get_db()` 工厂函数，要获取连接必须再加一对括号调用 `_get_db()`。直接写 `db = make_db(PA_DB)` 然后 `db.execute(...)` 会出错，且异常被 `app.py` 的 `try/except` 吞掉。
**修复**：模块级创建 `_get_svc_db = make_db(PA_DB)`，所有数据库操作处调用 `db = _get_svc_db()`。
**教训**：
  1. 使用 `make_db` 的正规模式：`_get_db = make_db(path)` 然后 `conn = _get_db()`。
  2. `try/except` 不要吞掉异常——至少要 `print(traceback.format_exc())`，否则上线后完全看不出问题。
  3. 这个 bug 本地测试不会发现，因为本地没有对应的目录结构。

### 7.27 代码审查发现的关键问题（2026-07-27）
1. **`prebuild_status()` 引用未定义变量 `data`**：第 385 行 `_STATUS_CACHE['data'] = data`，`data` 未定义。该函数被 `except Exception: pass` 吞掉，导致状态缓存预构建从未成功。修复为调用 `_build_server_status()`。
2. **PA 续期重复 HTTP 请求**：`_do_renew()` 中 `_pa_get_status()` 已请求一次 `webapps_url`，之后 token 缺失时又请求一次（第 548 行），`_pa_do_extend` 又请求一次（第 556 行），同一 URL 最多被请求 4 次。修复：`_pa_get_status` 同时返回 HTML，后续复用。
3. **`_read_log()` 缓存并发不安全**：`_LOG_CACHE` 在多线程读写（续期线程写 + API 线程读），无锁保护。修复：添加 `_LOG_CACHE_LOCK`。
4. **`renqing.py` 中 `_get_db` 定义在 `init_db()` 之后**：虽然运行时不会出错（模块加载完才调用），但代码顺序易引发误解。修复：移到 `init_db()` 之前。
5. **`_load_auth()` 无异常处理**：文件读取失败会导致整个 app 无法启动。修复：加 `try/except`。
6. **模块目录常量三处重复定义**：`deploy.py`（2 处）和 `backup.py`（1 处）各自定义 `KNOWN_MODULE_DIRS`。修复：统一提取到 `utils.py`，定义 `KNOWN_MODULE_DIRS`、`INTERNAL_DIRS`、`SYSTEM_RESERVED_DIRS`、`ALL_SAFE_DIRS`。

### 7.28 CSS 变量统一规范（2026-07-27）
每个 HTML 页面的 `<style>` 标签顶部必须包含 `:root{}` CSS 变量块，定义本项目统一变量名：

```css
:root{--primary:<主题色>;--bg:#f0f2f5;--card-bg:#ffffff;--text:#1e293b;--text-secondary:#64748b;--border:#e2e8f0;--shadow:0 2px 8px rgba(0,0,0,.06)}
```

各模块的 `--primary` 值：导航 `#4f46e5`、倒计时 `#8b5cf6`、绩点 `#059669`、成绩 `#7c3aed`、记账 `#4f46e5`、人情 `#dc2626`、部署 `#4f46e5`。

### 7.29 前端表单动态生成原则（2026-07-27）
当 HTML 表单中存在 3 个以上结构相同、仅数据不同的输入组时，必须抽取为 JS 数据驱动生成，而非手写重复 HTML：

- 数据源用 JS 数组/对象定义（如 `SUBJECTS` 数组）
- HTML 仅留容器元素（如 `<div id="xxxContainer"></div>`）  
- 页面加载时 JS 遍历数据源生成表单 DOM
- 生成的元素 ID 必须与原有 ID 完全一致（保证下游函数无感知）

收益：减少 HTML 行数、新增/删除项目仅改数据源、消除复制粘贴出错风险。


### 7.31 极简化 CSS 重构原则（2026-07-27）
CSS 代码量优化目标：300+ 行 CSS 可精简至 100 行以内，关键原则：

1. **天然继承**：body 统一字体、颜色、字号，子元素自动继承，不需逐个定义
2. **padding 体系精简**：banner 16px、section 16px 12px、card 14px，3-4 个值覆盖全部场景
3. **消灭独立 gap**：flexbox gap 功能可用父容器 padding + 子元素 margin-bottom 替代
4. **移除冗余**：不必要的 height、width:100% (块级默认)、opacity:1 (默认)、transform:scale(1) (默认)
5. **合并边框**：下划线和容器背景色合二为一，用背景色实现分隔线效果
6. **变量化**：颜色、字号、间距提取 CSS 变量，方便全局统一调整

### 7.32 PA 免费账户不支持 Scheduled/Always-on tasks（2026-07-28 已踩坑）
**现象**：想把后台续期、备份、清理从 Flask daemon 线程迁移到 PA Tasks，但 Tasks 页面显示「Scheduled tasks are only enabled for paid accounts」「Always-on tasks are only enabled for paid accounts」
**根因**：PA 免费账户不提供任何形式的定时/常驻任务，后台工作必须依附于已启动的应用进程
**后果**：
- 迁移方案不可行，必须回退改动
- 若强行禁用 daemon 线程，会导致续期、自动备份全部停止
**对策**：
- 免费账户继续使用 Flask daemon 线程驱动后台任务（已在 `app.py` 的 `_SAFE_STARTS` 中配置）
- `daily_task.py` 脚本保留但仅作「手动运行」或「未来升级付费账户后迁移」使用，不可作为免费账户默认方案
- 付费账户可配置 PA Scheduled Tasks，命令：`python /home/Gjx/toolbox/daily_task.py`
**原则**：设计定时任务迁移前，先确认目标环境的功能可用性，避免先改代码再发现平台限制

### 7.33 移动端双 Sticky 层过高遮挡内容（2026-07-28）

**现象**：页面既有 sticky header 又有 sticky 标签栏，两者叠加在手机小屏上可能占据超过 1/3 的可用高度，导致内容区被挤压缩，底部按钮难以触达。

**根因**：header（~50px 含 safe-area）+ tab-bar（~44px）叠加后可达 ~94px，在 667px 高的 iPhone 8 上占 14%，在刘海屏机型（812px）上含 safe-area（~44px）可达 ~138px，占 17%。内容区用 `calc(100vh - 150px)` 硬编码高度，不同机型误差极大。

**后果**：
- 移动端可滚动内容区过矮，用户需频繁滚动
- 编辑区 textarea 高度不足，大屏记事体验差
- 双栏布局在部分安卓设备上编辑区完全不可见

**对策**：
- 笔记本+待办这种双面板页面，移动端使用 `flex-direction: column` 替代双栏，给列表区 `max-height` 限制而非固定高度
- 使用 `flex: 1` 让内容区自动填充剩余空间，而非 `calc(100vh - 固定值)`
- 桌面端 (>768px) 保留固定高度 `calc(100vh - 180px)` 实现双栏撑满
- 所有 sticky 层级加起来不超过 2 层

### 7.34 模块合并时的旧文件清理（2026-07-28）

**现象**：将待办和记事合并为统一模块后，旧路由文件 `routes/todo.py`、`routes/notepad.py` 和旧页面 `记事/index.html` 仍存在于仓库中，导致代码库混乱。

**对策**：
- 模块合并时必须同步删除被替代的旧文件（路由文件、HTML 文件、静态资源）
- 检查 app.py 中是否有旧模块的路由注册残留
- 检查 utils.py 的 `FOLDER_MAP` 和 `KNOWN_MODULE_DIRS` 是否同步更新
- 检查 `PUBLIC_PREFIXES` 免登录白名单是否清理旧 API 前缀

### 7.35 PA 免费账户 CPU 使用注意事项（2026-07-28）

PA 免费账户每日 CPU 配额约 **100~120 秒**，超出后应用可能被限流或暂停。

**当前后台任务 CPU 消耗**（均在安全范围内）：

| 线程 | sleep 间隔 | 执行耗时 | 日均 CPU |
|------|-----------|---------|----------|
| PA 续期 | 24h / 动态 | 5~10s (HTTP+HTML解析) | 极低 |
| 自动备份 | 最短 1h | 1~3s (zip 6 个 db) | 极低 |
| 自动清理 | 7 天 | 1~5s (文件遍历+截断) | 极低 |

**核心原则**：

1. **后台任务 sleep 不低于 3600 秒**：当前所有线程最小间隔为 1 小时，符合安全标准。禁止引入秒级短间隔轮询（如 `sleep(10)` 每秒检查），否则凭空消耗 CPU 配额
2. **续期操作用异步线程执行**：续期（HTTP 请求 + HTML 解析）是耗 CPU 最大的操作，当前使用 `threading.Thread(target=..., daemon=True)` 异步执行，不阻塞主进程
3. **关注 PA Account 页面的 CPU 图表**：正常模式应接近零使用（sleep 期）+ 偶发微小尖峰（请求处理）。若出现持续高位或每日明显增长，说明某个循环出了 BUG
4. **worker 生命周期风险**：PA 免费 worker 可能在不活跃期间被平台回收。如果在 CPU 密集型操作（如续期）中途被 kill，操作会中断但不丢数据（下次检查会重试）

**新增后台任务的检查清单**：
- sleep 间隔是否 >= 3600 秒？
- 是否有超时保护（HTTP timeout、文件操作上限）？
- 操作失败时是否有重试+退避机制？
- 是否使用 daemon 线程避免阻塞 worker 退出？

### 7.36 Windows 命令行中文路径与编码坑（2026-08-18）
**现象**：命令行里带中文目录的操作容易失败；`python -c "..."` 内嵌中文路径乱码（如 `Qoder椤圭洰`）。
**根因**：
- PowerShell 5.1 不支持 `&&` 命令链（必须用 `;` 或写成独立命令）
- 命令行参数在 UTF-8/GBK 间转换时，中文路径被破坏，Python 收到乱码路径
- 子进程输出捕获后 `print()` 到 GBK 控制台会抛 `UnicodeEncodeError`
**对策**：
- 含中文路径的命令一律通过「ASCII 路径的辅助脚本」执行：脚本文件放 Temp 目录（如 `C:/Users/xxx/AppData/Local/Temp/chk.py`），脚本内部用 Unicode 字符串路径 + `subprocess.run(..., cwd=..., encoding='utf-8', errors='replace')`
- 脚本开头加 `sys.stdout.reconfigure(encoding='utf-8', errors='replace')`
- 验证：写一个临时脚本 `os.listdir` 目标目录，确认路径解析正确后再继续
**本项目适用场景**：所有模块目录名均为中文（人情/绩点/记账等），涉及这些目录的命令行操作都适用。

### 7.37 git 中文路径八进制转义导致误判（2026-08-18）
**现象**：`git show HEAD --name-status` 输出中，中文路径显示为 `\344\272\272\346\203\205/...` 八进制转义。用文本匹配「人情」字面统计提交文件时，结果只命中 1 条（commit 标题），误判「文件未提交」。
**根因**：git 默认 `core.quotepath=true`，非 ASCII 路径转义输出。
**对策**：判断文件是否被提交用 `git ls-files <相对路径>`（输出正常显示文件名）或 `git status --short`，不要用文本匹配 `--name-status` 的输出。需要真实文件名时可加 `-c core.quotepath=false`。
**本项目适用场景**：仓库内所有模块目录名均为中文，`git status` / `git show` 的输出都受影响。

### 7.38 PowerShell 中文参数乱码 → 提交信息改用文件传入（2026-08-18，2026-09-12 补充）
**现象**：PowerShell 里执行 `git commit -m "中文信息"`，中文提交信息存进仓库后变成乱码；含中文路径的命令也可能解析失败。
**根因**：PowerShell 把命令传给临时 .ps1 再执行，中文参数在 UTF-8 → GBK 转换时被破坏。
**对策**：
- **git 提交信息不要用 `-m "中文"`**：改为写入 UTF-8 文件后 `git commit -F 提交信息.txt`，提交完删除该文件（已实测可靠）
- 含中文路径的命令改用 Python 辅助脚本执行（`subprocess.run` + `capture_output` + UTF-8 解码）
**验证**：2026-09-12 连续两次提交，用 `-m "中文"` 的那次 `git log` 显示为乱码，用 `-F 文件` 的那次显示正常。
**延伸**：如需整理提交历史（如误提交临时脚本），可用 `git reset --soft HEAD~N` 把多个提交合并成一个后再重新 commit，安全（不动工作区与索引内容）。

### 7.39 批量替换误伤辅助函数自身导致无限递归（2026-09-12）
**现象**：用 `replace_all` 把 35 处 `return jsonify({'success': False, 'error': str(e)}), 400` 统一替换为 `return _fail(e)` 后，`_fail()` 函数体内那一行也被替换，变成 `return _fail(e)`，调用即触发 `RecursionError: maximum recursion depth exceeded`。
**根因**：批量替换是纯文本匹配，新写的辅助函数含有与替换目标完全相同的代码行，被一并改写。
**对策**：批量替换后检索旧模式（如搜索 `str(e)`）确认已清零，并**单独复查辅助函数本体**；更稳妥的顺序是先替换所有调用点，最后再落地辅助函数定义。
**验证**：本次会计模块接口测试中「重复载入示例账套」触发 RecursionError，修复后正确返回 400 与提示文案。

---

### 7.41 新模块目录被自动清理删除 → 必须登记进 `KNOWN_MODULE_DIRS`（2026-09-14）

**现象**：新模块部署后页面与 `manifest.json`/图标全部 404；部署接口返回成功、`git log` 已是最新提交，但服务器上该目录根本不存在，过一会儿 `git status` 又出现 ` D "新模块/index.html"`。手动 `git checkout` 恢复后短时间内再次消失，反复无常。

**排查过程（重要）**：先按「`git reset --hard` 不落盘」的思路加了恢复逻辑，但部署时检测「无缺失」，隔几分钟再查又缺失——说明**文件是在 reset 之后被某个后台任务删掉的**，不是 git 的问题。用临时诊断路由对比「reset 后立刻查」与「稍后查」才锁定真凶。

**根因**：`routes/backup.py::_do_cleanup()` 第 7 步会「动态检测并清理无关联模块的遗留目录」——遍历仓库根目录，凡不在 `ALL_SAFE_DIRS`（= `KNOWN_MODULE_DIRS | INTERNAL_DIRS | SYSTEM_RESERVED_DIRS`）白名单里的一级目录，直接 `shutil.rmtree()`。新模块只写了代码、路由、导航入口，忘了把中文目录名登记进 `routes/utils.py::KNOWN_MODULE_DIRS`，于是每次自动清理都把整个模块目录删掉。

**对策（新增模块必做）**：
1. 新建中文模块目录后，**第一时间把目录名加入 `routes/utils.py` 的 `KNOWN_MODULE_DIRS`**（若带数据库，还要看 `deploy.py::KNOWN_DB_DIRS` 与 `backup.py` 的备份清单）；
2. 部署流程保留一道兜底：`routes/deploy.py::_restore_missing_files()` 用 `git ls-files -z` 与磁盘逐条比对，缺失项 `git checkout-index -f -z --stdin` 强制还原（不要只看 `git status --porcelain`，状态缓存可能把缺失文件报成「干净」）；
3. 今后新增模块的自查清单应包含「目录白名单登记」这一项。

**验证**：把新模块目录加入白名单并部署后，该模块的 `manifest.json` 与两个图标持续返回 200，跨多次自动清理不再丢失。

---

### 7.42 本地 git 无法 push（`git-remote-https` 缺失）→ 用 dulwich + 凭据管理器推（2026-09-14）

**现象**：本机 `git push` 报 `remote-https is not a git command`；`git credential fill` 也直接崩溃，无法走常规 HTTPS 推送。

**根因**：本地 Git 安装不完整，缺少 `git-remote-https.exe`（HTTPS 传输助手），命令行 Git 完全走不通 HTTPS 远程。

**对策**：改用纯 Python 的 `dulwich` 库推送：用 `ctypes` 调 Windows 凭据管理器 `CredReadW` 读取 `git:https://github.com` 里保存的 token 作为密码，再 `dulwich.client.HttpGitClient.send_pack` 推送到 `master` 与 `main` 两个分支。注意 dulwich 不会自动更新远端非快进引用，需显式给出旧/新 commit id。

**验证**：本次连续 4 次提交（`288a4bf`、`5cd2042`、`85b0c25`、`e620682`）均成功推达 GitHub 两个分支，服务器随后能拉到最新代码。

**顺带提醒**：PowerShell 里 `curl` 是 `Invoke-WebRequest` 的别名，参数不兼容（`curl -s -o NUL -w` 会报「缺少参数 SessionVariable」），要调真实 curl 必须写 `curl.exe`。

---

## 八、关键技术选型说明

| 选项 | 选择 | 原因 |
|---|---|---|
| 框架 | Flask | 轻量、适合 PA 免费环境、学习成本低 |
| 数据库 | SQLite | 零配置、单文件、PA 环境原生支持 |
| 前端 | 原生 HTML/CSS/JS | 无需构建工具、零依赖、加载快、PA 磁盘省空间 |
| 路由模式 | Flask Blueprint | 模块隔离、独立注册、容错导入 |
| 部署 | PA Web App | 免费、稳定、支持 git 自动部署 |
| 日志 | 按模块分文件 | 便于定位问题、不影响性能 |
| 认证 | Session + Token | Session 用于前端访问，Token 用于 API 调用 |

---

## 九、开发清单（Checklist）

每次新增模块或修改现有模块后，按此清单检查：

```
□ python -m py_compile 每个修改的 .py 文件
□ 本地导入测试：from routes.xxx import bp - 必须成功
□ 本地接口测试：Flask test_client 测试所有 API（不限于修改的模块）
□ 未登录访问测试（如有免登录路由）
□ 异常输入测试：空参数、非法值、超长数据
□ 数据库连接检查：确认使用 make_db() 工厂，无直接 sqlite3.connect()
□ SQL 注入检查：用户输入参数必须用 ? 占位符
□ 敏感值检查：Token/密码是否硬编码，应使用 os.environ.get()
□ f-string SQL 检查：确认拼接值来源（内部可控 vs 用户输入）
□ 数据库连接关闭检查：所有 conn = _get_db() 后是否有 conn.close()（含异常路径）
□ app.py 中正确添加蓝图注册和 init_db 调用
□ 前端 HTML 路径与 send_from_directory 一致
□ 日志文件路径正确，目录存在
□ 移动端适配检查：375px/390px/768px 三种宽度无溢出
□ 表单输入框、按钮、下拉选择触控区域达标（>=44px）
□ 返回箭头使用 flex 布局，非 absolute 定位（规范 5.6）
□ 底部导航栏纯文字无符号，font-size:14px; font-weight:700（规范 5.7）
□ 页面有 favicon，theme-color 与主色调一致（规范 5.8）
□ 等宽字体/长文本元素有 word-break 溢出保护（规范 5.9）
□ Header 包含 safe-area-inset-top 适配（规范 5.5）
□ 组件样式符合全局标准（按钮/输入框圆角 10px、卡片 padding 16px、min-height 44px）
□ git add + commit + push
□ 触发部署 API + 等待重载
□ 线上接口测试，确认功能正常
□ 确认部署日志无异常
□ 新增模块同步检查：按 3.5 同步清单逐项核对（概览页/备份/清理/FOLDER_MAP/KNOWN_MODULE_DIRS/KNOWN_DB_DIRS/allowed_prefixes 等 14 项）
```

---

## 十、版本与维护记录

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-07-19 | 建立规范文档 | 总结开发经验，统一规范 |
| 2026-07-19 | countdown.py 缩进修复 | IndentationError 导致全站 500 |
| 2026-07-19 | app.py 容错导入 | 防止单个模块崩溃拖垮全站 |
| 2026-07-19 | 部署 API 优先机制 | 确保部署 API 始终可用 |
| 2026-07-19 | 全面代码审查 | gpa/hsgrades 统一 make_db；DEPLOY_TOKEN 迁移环境变量 |
| 2026-07-19 | 规范文档 v2 | 新增：敏感配置管理、make_db 强制规则、SQL 拼接安全规则、3 个新坑点、审查清单扩充至 17 项 |
| 2026-07-19 | 规范文档 v3 | 新增：5.3 移动端适配强制规范（布局/触控/字体/安全区域/表格/弹窗/测试要求），审查清单新增 2 项移动端检查 |
| 2026-07-19 | 规范文档 v4 | 新增：6.5 PythonAnywhere 平台管理（账户信息/Web配置/部署API原理/免费限制/运维命令） |
| 2026-07-19 | 规范文档 v5 | 新增：3.5 新增模块同步清单（12 项强制检查）。修复：记账模块同步补全（概览页入口/备份/FOLDER_MAP/KNOWN_DB_DIRS/allowed_prefixes） |
| 2026-07-19 | 记账系统 v3 | 综合优化：修复 ::root CSS 笔误，前端搜索框接入，还款对冲机制（快速还款按净值判断收支方向），事件净值归零自动结清，统计页饼图（Canvas 甜甜圈），账户间转账（双记录），月度预算设定+完成度条，分类支持自定义（categories 表+CRUD API），inputmode 优化移动端键盘 |
| 2026-07-19 | 移除月度预算 | 撤销月度预算功能（后端 budgets 表+3 API + 前端进度条），用户不需要 |
| 2026-07-19 | 移除饼图/搜索框 | 撤销统计页饼图和记录页搜索框 |
| 2026-07-19 | 记账页大屏适配 | content 区 max-width 600→1400px，统计面板 auto-fit 自适应列 |
| 2026-07-24 | 人情归档查找修复 | 修复 4 处 `events.find()` 未按 `isUnarchivedMode` 在正确列表中查找的 bug，新增坑点 7.12 |
| 2026-07-24 | 人情模块页面拆分 | 将原单页拆为工作台(index.html，只编辑未归档事件)和归档库(archive.html，已归档事件档案浏览)两个独立页面，新增 `/renqing/archive` 路由 |
| 2026-07-24 | Git 推送流程优化 | 合并 push 为一条命令：`git push origin master main`（同时推送两个分支），避免手动单独同步 main 分支 |
| 2026-07-24 | 提交部署原子化 | git push 和触发部署合并为不可拆分的原子步骤，以后每次提交后自动连续执行部署，无需手动触发 |
| 2026-07-21 | 记账模块财务术语标准化 | 全面重构用户界面用词：账户→交易对象、收入→收款、支出→付款、他欠我→应收、我欠他→应付、事件→事项、净值→结余、还款→核销。保留后端API值不变，前端通过label映射显示新术语 |
| 2026-07-20 | 记账页移动端适配 | 375px 小屏表单竖向堆叠防溢出，viewport-fit=cover，safe-area 安全区域，触控目标≥44px，输入字号 16px，768px 断点媒体查询 |
| 2026-07-20 | 人情系统全量移动端自检修复 | Toast 移动端 top:auto 防拉伸；保存页 Promise.allSettled 并行化（12条从串行12次→单次并行）；原生 confirm 替换为自定义弹窗；弹窗打开锁 body 滚动；card-title 内联样式移入 CSS 类；loadEventDetail 并行获取记录+事件列表；锁定切换轻量刷新不重载 |
| 2026-07-21 | 规范文档 v6 | 5.3 移动端适配新增核心原则：所有模块移动端必须实现 App 效果（全屏沉浸布局、底部导航、滑动删除、下拉刷新、弹窗全屏），通过 @media (max-width: 768px) 与桌面端布局分离 |
| 2026-07-21 | 规范文档 v7 | 5.3 扩展为双模式设计：移动端App效果+PC端大屏显示，统一断点768px，底部导航仅移动端显示，PC端顶部导航+max-width居中 |
| 2026-07-23 | 代码规范统一 v8 | renqing.py/paiban.py 改用 make_db()（消除自定义 _db() 函数）；renqing API 返回格式 ok→success（与其他模块统一）；gpa.py/hsgrades.py 移除冗余 PRAGMA（make_db 已设置）；人情前端 r.value.ok→r.value.success。收益：5 个文件/-23 行冗余代码，数据库连接方式全部统一 |
| 2026-07-23 | 错误格式统一 v9 | renqing.py 16 处 {'error': '...'} 改为 {'success': False, 'error': '...'}，与其他所有模块保持一致。前端已有 e.error 取值因此无需改动。至此所有模块 API 返回格式完全统一 |
| 2026-07-23 | 事件归档机制 v10 | 礼金管理新增归档概念：events 表增加 archived 字段(Schema v4)，新增 POST /events/<id>/archive 接口；前端事件列表默认折叠、仅显示已归档事件、新增"归档"按钮和"待归档"切换视图。未归档事件不进入列表，归档后才收录 |
| 2026-07-24 | 礼金系统全面审查修复 | 1) savePage 中 `r.value.success`→`r.value.ok`（Response 对象无 .success 属性，导致保存失败静默误报）；2) 抽取公共 JS 模块 renqing-common.js，两个 HTML 各减少约 100 行重复代码；3) update_record 增加金额校验（与 add_record 对齐）；4) 归档库日常随礼排除当前年份；5) 新增加载状态旋转动画（保存/刷新时显示） |
| 2026-07-24 | 人情 PWA 图标与 manifest | 仿照 countdown 模块，新增绿色渐变背景白色"情"字 SVG 图标（192/512）、manifest.json、静态资源路由及 head 标签；将 `/renqing/manifest` 和 `/renqing/icon` 加入 PUBLIC_PREFIXES 免登录，保证手机端"添加到主屏幕"能正常读取图标与 manifest |
| 2026-07-24 | 项目全面质量改进 v11 | 1) AUTH_HASH 全局变量加 `threading.Lock` 读写保护；2) `reload_webapp.py` 密码解码复用 `utils.decode_pw()`（消除重复 XOR 逻辑）；3) `pa.py _do_renew()` 拆分为 `_pa_login`/`_pa_get_status`/`_pa_do_extend`/`_pa_verify_result` 四个子函数；4) 密码编码升级为 PBKDF2(200k次迭代)+HMAC 完整性校验（`v2:`前缀），兼容旧 `ENC:` 格式；5) `routes/utils.py` 新增 `JsonStore` 通用类，消除 paiban/gpa/hsgrades 的 JSON 数据存取重复代码；6) `countdown.py` 抽取 `_validate_event_dates()` 消除 create/update 事件校验重复（-45行）；7) `accounting.py` 抽取 `_validate_record_data()` 消除流水校验重复（-25行）；8) paiban/成绩模块新增 PWA manifest（补齐移动端"添加到主屏幕"支持）。 |
| 2026-07-25 | PA续期调度修复 | PA 续期下次执行时间基于上次实际执行时间+周期天数，而非取日期部分后算凌晨 0 点 |
| 2026-07-25 | 备份调度逻辑修复 | 备份/清理线程启动时检查上次执行时间，超间隔立即执行而非先睡 24 小时/7 天（解决 PA 免费版频繁重启导致线程永远等不到执行窗口的问题） |
| 2026-07-25 | 部署规范 v12 | 新增 6.2 节「自动推送部署原则」：代码修改后自动 git push + 部署为强制铁律，中间不中断；明确 Windows PowerShell 需用 curl.exe、原子操作不可拆分 |
| 2026-07-25 | accounting CHECK 约束迁移修复 | 修复 categories 表 CHECK 约束变更死胡同：旧 CHECK 拒绝新 type 值 + 新 CHECK 拒绝旧 type 值。改用无约束临时表中转+type 值映射，get_categories 防御性处理未知 type。新增坑点 7.16 |
| 2026-07-25 | 宕机隐患全面排查与修复 | 全面扫描所有 Python 文件：发现 pa.py init_db 无 try/except、records 表 CHECK 约束同款死胡同、renqing.py Excel 导入提前 return。修复所有高危项，新增坑点 7.17（init_db 规范）和 7.18（隐患排查方法论） |
| 2026-07-25 | 重载稳定性优化 | reload_webapp.py：API重载增加3次重试+退避（3/6/9s）、重载后主动轮询验证服务可用性(30s内)、add 网络抖动处理。新增坑点 7.19（PA服务重载真空窗口） |
| 2026-07-26 | 手机导航页 | 新增 /nav 导航首页，PA 渐变 Logo + 深色卡片网格 + PWA manifest/icon 支持添加到手机主屏幕。首页 / 改指向 /nav |
| 2026-07-27 | 性能自审查与优化 | 全面排查启动/运行时性能瓶颈，发现并修复 4 项问题：1) 状态查询 9 次重复磁盘扫描→单次遍历；2) 移除状态查询中 7 个 DB 各次的冗余 PRAGMA WAL；3) prebuild 延迟 30s→5s；4) 记账 init_db 添加 PRAGMA user_version 跳过已完成迁移检查。新增坑点 7.20（重复 I/O）、7.21（迁移版本标记）、7.22（缓存延迟与 worker 生命周期）。本次优化预计将 PA 启动后首次状态查询耗时降低 50% 以上。 |
| 2026-07-27 | PA 重载卡死修复 | 根因：后台线程+缓存预构建堵在模块 import 阶段，超过 PA 健康检查超时。修复：后台线程改为延迟启动（sleep 3s），去掉 _diag flush，新增 OSError 静默拦截器。新增坑点 7.23（启动阶段禁止同步非关键初始化）、7.24（write error 不触发错误日志链）。 |
| 2026-07-27 | 服务管理器功能 **[已取消]** | 曾新增 `routes/service_manager.py` 统一管理后台服务启停，后续取消，文件未保留。新增坑点 7.25（服务管理器架构规范，已标记为取消）。 |
| 2026-07-27 | make_db 工厂函数教训 | `make_db()` 返回工厂函数而非连接对象，直接当连接用会报错。经验独立保留于坑点 7.26（不再关联已取消的 service_manager）。 |
| 2026-07-27 | 全面代码审查及修复 | 发现并修复 6 个关键问题：prebuild_status 未定义变量（缓存从未生效）、PA 续期重复 HTTP 请求（最多 4 次）、日志缓存无锁、renqing.py _get_db 位置、_load_auth 无异常处理、模块常量三处重复。新增坑点 7.27。 |
| 2026-07-27 | 配色主题全面升级 | 深色主题（`#0f172a`底色）全面改为简约时尚浅色常规配色（`#f0f2f5`底色+`#ffffff`卡片+`#1e293b`文字）。主色调统一为鲜明 indigo（`#4f46e5`）。更新 8 个 HTML 文件+6 个 manifest.json。5.2 节补充完整配色规范表。|
| 2026-07-27 | 系统全面修复 v13 | 全系统评估后实施 10 项修复：1) CSS `::root` 笔误确认无误（单冒号正确）；2) 人情模块 API 函数提取到 renqing-common.js（fetchRetry/apiGet/apiPost/apiPut/apiDelete），index.html(-13行)、archive.html(-8行)；3) 成绩模块表单模板化：6个科目表单由 SUBJECTS 数组动态生成（HTML -136行 + JS +25行）；4) Chart.js 版本统一为 4.4.7；5) 记账删除函数合并（deleteRecord 集成 doDeleteRecord）；6) 全局 CSS 变量统一：绩点/成绩/部署/导航 4 个页面新增强制 `:root` 变量块；7) 新增 API 版本端点 `/api/version`；8) 速率限制持久化（SQLite + 内存，启动恢复+定时刷新）；9) 新增单元测试 tests/test_app.py（7 个 TestCase 类覆盖全部模块）；10) 开发规范补充本次维护记录。新增坑点 7.28（CSS 变量统一规范）、7.29（前端表单动态生成原则）。 |
| 2026-07-27 | P0/P1修复+文档清理 | `/api/version` 500修复（_LOADED_MODULES）、测试端点 /api/deploy-ping→/api/ping、人情 CSS 变量+viewport 对齐、service_manager 文档标记已取消。 |
| 2026-07-27 | 代码精简与性能优化 | 1) renqing.py：toggle_lock/archive 合并为 _toggle_event_flag（-20行）、_adjust_for_flow 改用 CASE WHEN 批量 UPDATE 替代逐行循环（N次SQL→1次）、list_records 消除中间变量 evt_name；2) gpa.py：提取 _safe_json_load 消除 4 处重复 json.loads+try/except（-8行）、简化 get_data/save_data 中 conn=None 冗余模式；3) hsgrades.py：同步引入 _safe_json_load、简化 init_db conn=None 模式；4) countdown.py：简化 init_db conn=None 模式、list_events date_desc 统一 leap_prefix 变量；5) deploy.py：_needs_reload STATIC_EXTS tuple→set、消除冗余 PY_EXTS 常量；6) app.py：_deferred_starts 修复 for-else 语义 bug。总计净减少约 40 行代码。 |
| 2026-07-28 | UI 细节统一系列 | 1) 记账返回箭头从 absolute 定位改为 flex 布局；2) 移动端所有导航字体统一为 `14px/700`（激活态 `800`）；3) 返回箭头样式全平台统一（`color:#64748b; font-size:20px; min-width/min-height:44px`）；4) 部署页移动端导航/侧边栏适配；5) 记账 header 加 `safe-area-inset-top` 防状态栏遮挡；6) PA 续期页移动端全面适配（状态卡片/周期按钮/表单/日志/历史记录）。 |
| 2026-07-28 | PA 续期页布局弹跳修复 | 日志框等宽字体长行撑破 768px 断点导致页面弹回大尺寸。修复：`.pa-log-box{word-break:break-all;overflow-wrap:break-word}` + `.ctn{max-width:100vw;overflow-x:hidden}`。新增坑点 5.9（等宽字体防溢出规范）。 |
| 2026-07-28 | 全局风格统一大整改 | 30+ 处修改覆盖 6 个 HTML 文件，确立全局组件统一标准：CSS 变量名（`--card-bg` 非 `--bg-card`）、全局字体栈、header 样式（`12px 16px`）、按钮（`border-radius:10px; min-height:44px`）、输入框（`border-radius:10px; min-height:44px`）、卡片（桌面 `12px` 移动端 `10px`）。新增规范 5.5（全局组件统一样式标准）、5.6（返回箭头规范）、5.7（底部导航栏规范）。 |
| 2026-07-28 | Favicon/LOGO 统一 | 导航主页补 favicon（`link rel="icon"`），登录页和设置密码页添加内联 SVG favicon。新增规范 5.8（Favicon/LOGO 浏览器标识规范）。 |
| 2026-07-28 | 成绩导航符号清理 | 移动端成绩页底部导航栏移除 `~` 和 `*` 符号，改为纯文字「概览」「成绩」。遵循 5.7 纯文字规范。 |
| 2026-07-28 | 开发规范文档 v14 | 基于历次交流总结经验，补充 5.5~5.9 共 5 个新规范节（组件标准/返回箭头/底部导航/favicon/等宽防溢出），新增 7 条维护记录。开发规范文档现已达 150+ 行条目。 |
| 2026-07-28 | 全面代码审查 v2 | 审查 11 个 Python 文件，修复 2 严重 + 5 中等 + 8 轻微问题：1) 移除 app.py 硬编码 DEPLOY_TOKEN 默认值（安全漏洞）；2) countdown.py init_db 补 try/except；3) deploy.py /api/status 和 /api/pa-summary 返回补 success 字段；4) pa.py status()/log() 补异常处理；5) _safe_json_load 重复函数移至 utils.py（gpa/hsgrades 改为 import）；6) 裸 except 改 except Exception；7) deploy.py DB 连接异常路径补 finally close；8) pa.py 多余空行清理。全部通过 py_compile 语法检查。 |
| 2026-07-28 | PA Scheduled Tasks 迁移尝试与回退 | 方案：新增 `daily_task.py` 独立脚本、pa.py/backup.py 导出公开接口、app.py 禁用 daemon 线程。实施后用户发现 PA 免费账户 Tasks 页面提示「仅付费账户可用」。回退：恢复 app.py `_SAFE_STARTS`，daemon 线程继续运行。`daily_task.py` 保留，供手动执行或未来升级付费账户后使用。新增坑点 7.32（PA 免费账户限制）。 |
| 2026-07-28 | PA CPU 使用分析 | 梳理三个后台线程（续期/备份/清理）的 CPU 消耗，确认均在安全范围内（日均 < 1 秒）。确立后台任务安全准则：sleep 不低于 3600s、HTTP 操作加超时、异步执行不阻塞主进程。新增坑点 7.33（PA CPU 注意事项）。 |
| 2026-07-28 | 全平台 header 统一大整改 | 所有模块 header 强制统一为标准模板，杜绝各模块 header 差异：1) 人情/index.html 移除 box-shadow；2) 人情/archive.html padding 从 `0 20px` 改为 `12px 16px`，移除 box-shadow，补 safe-area-inset-left/right，新增返回箭头 `←`；3) 倒计时 `div.hdr` 改为 `<header>` 标签，移除 box-shadow；4) 待办返回箭头 `&lt;` 改为 `←`，补 safe-area-inset-left/right；5) 成绩移除 box-shadow；6) 记账移除 box-shadow。确立规范 5.10「全平台 UI 一致性铁律」。 |
| 2026-07-28 | 人情刘海屏遮挡根治 | 人情模块 header 从 `padding-top: calc(12px + env(safe-area-inset-top))` 改为 body `padding-top: env(safe-area-inset-top)` + `body::before` 伪元素兜底遮罩。根本原因：`env(safe-area-inset-top)` 在部分 iOS 版本可能返回 0，仅靠 header calc 不可靠。新方案通过 body padding 推下全页面内容，`::before` 固定白条填满刘海区域，双重保障。同步更新规范 5.10 的 header 标准模板。绩点 header 补移除遗留的 box-shadow。 |
| 2026-09-12 | 规范文档清理 | 移除与「卧龙吟辅助」桌面项目相关的全部资料：专用坑点（原 7.38~7.44、7.46~7.53，涉及 PyInstaller 打包、Playwright 线程模型、Cocos H5 游戏自动化等）及维护记录 7 条（2026-08-18~08-19）；并移除已废弃的「网速测试模块」「字帖生成器」历史记录条目。保留三条对本项目仍适用的通用坑点：7.36（Windows 中文路径与编码）、7.37（git 中文路径八进制转义）、7.38（PowerShell 中文参数乱码，由原 7.45 重编号，对策更新为「提交信息写入 UTF-8 文件后用 `git commit -F` 传入」）。 |
| 2026-09-12 | 会计模块易用性改造 | 按「更规范、好用、易上手」评估实施 6 类改动：1) **内置场景模板** 9 个（销售收款/赊销/收回欠款/进货付款/赊购/付货款/费用支出/发工资/借入款项），把复式记账降维为「选场景 + 填金额」，移动端新增模板选择弹层，PC 端模板页与编辑器下拉均可套用（★ 前缀标识）；2) **示例账套** 新增 `POST / DELETE /api/ledger/demo`，写入 10 张带 `demo` 标记的凭证（日期均匀落在当月已过日期，避免未来日期），可一键清除且不影响用户自录数据；3) **新手引导** 两端在空账套时显示「三步开始记账」卡片（设期初 → 建往来 → 记凭证 + 载入示例，可关闭并 localStorage 记忆）；4) **录入效率** 移动端新增「按差额补平」、常用科目置顶（最近 6 个）、两端共用摘要历史候选（`datalist`，localStorage）；5) **一致性修复** PC 端主色 `#2563eb`→`#0d9488` 与移动端/manifest 统一、顶栏补「← 返回导航」、凭证编辑器关闭加未保存确认、附件列与张数补说明、移动端触控目标补齐 44px、凭证列表补示例/红冲/作废标记；6) **后端加固** `vouchers.demo`/`accounts.demo` 增量列、5 个索引（含 `uq_vouchers_no` 唯一索引，容错创建）、统一错误响应 `_fail()`（业务错误 400 / 服务端 500，替换 35 处裸 400）。新增坑点 7.39（批量替换误伤辅助函数自身）。 |
| 2026-09-14 | 新增二维码生成模块 | 需求：把手机号做成二维码，扫码直接拨号。新增 `routes/qr.py` + `二维码/index.html`：1) **纯标准库实现 QR 编码**（字节模式、版本 1~10、纠错 L/M/Q/H），不依赖 `qrcode`/`Pillow` 等第三方包——PA 免费版不保证 pip 安装成功，Reed-Solomon 纠错与 PNG（`zlib`+`struct`）全部自实现；2) **API** `GET/POST /api/qrcode`（fmt=svg/png/json）与 `/api/qrcode/capacity`，参数越界/内容超容量返回 400 并给出中文提示；3) **前端** 支持电话（`tel:`）/短信（`SMSTO:`）/网址（自动补 `https://`）/文本四类内容，纠错等级与图片大小可调，可下载 PNG/SVG、复制编码内容、保留最近 10 条历史（localStorage，不落库）；4) 配套 `manifest.json` + 192/512 SVG 图标，`/qrcode/manifest`、`/qrcode/icon` 加入 PUBLIC_PREFIXES 免登录；5) 导航页新增入口卡片（复用空闲的 c2 绿色样式）。**验证**：与 python-qrcode 库逐位对比 272 组（9 类内容 × 4 纠错等级 × 8 掩码）矩阵完全一致；OpenCV `QRCodeDetector` 解码 48 组 PNG 全部还原原文；11 条单元测试（`tests.test_app.TestQRCode`）全绿。新增坑点 7.40（二维码 PNG 必须横纵双向放大）。 |
| 2026-09-14 | 部署链路修复与推送方案 | 1) 线上二维码模块 404 排查：真凶是 `backup._do_cleanup()` 的遗留目录清理，新模块目录未登记进 `KNOWN_MODULE_DIRS` 被整体删除，已补 `'二维码'`（坑点 7.41）；同时在 `routes/deploy.py` 新增 `_restore_missing_files()` 作为兜底（`git ls-files -z` 与磁盘比对 + `checkout-index -f`），并在部署响应回显 `restore` 字段；2) 本地 git push 因缺少 `git-remote-https` 失效，改用 `dulwich` + Windows 凭据管理器 token 推送（坑点 7.42）；3) 清理临时诊断路由 `qrdiag`/`qrdiag2`/`gitdiag` 与临时推送脚本。 |
| 2026-09-14 | 二维码模块改为挪车电话码 | 按需求收敛为「只做电话二维码」：1) **后端** `/api/qrcode` 参数由 `text` 改为 `phone`（新增 `title`/`footer`/`hint`/`theme`/`intl`），号码统一规范为 `tel:+86…`，去掉短信/网址/文本三类内容；2) **美化 SVG** 新增渐变标题条 + 标题（默认「扫描挪车」）+ 号码行（可隐藏中间四位）+ 提示行，码点圆角、定位点做三层圆角同心框，3 套配色；无文案时仍输出纯码；3) **前端** 页面重做为「预览 + 号码文案 + 外观参数」，保存图片走 Canvas 合成（SVG→Image→PNG，3 倍分辨率，保留中文），失败自动退回后端纯码 PNG；4) 导航入口与 manifest 改名「挪车电话码」；5) 测试改为 13 条（含号码规范化、`intl=0`、卡片文案、纯码判定）。**验证**：自写 SVG 光栅化 + OpenCV 解码，4 尺寸 × 4 纠错等级 × 3 主题 + 纯码对照共 **64/64** 全部还原 `tel:+8613800138000`；调参过程中发现留缝/大圆角会掉识别率，已固化为常量并记录坑点 7.43。 |
| 2026-09-14 | 挪车码去掉号码、修复扫码不能拨号 | 用户反馈两点：二维码下方印号码没意义；扫码只显示文本、无法直接拨号。1) **去掉号码行**：`render_svg()` 删除 `footer` 参数与号码行布局，前端移除「号码显示方式」整块，卡片只剩标题 + 一行提示（新增常用提示语快捷标签）；2) **新增扫码模式 `mode`**：`vcard`（默认，联系人名片，微信/系统扫码器识别为名片后可点呼叫，名片名固定短名「挪车」以控制码密度）、`tel`（直接拨号，码最稀疏）、`page`（本站中转页 `/qrcode/call/<号码>`，免登录，页面用 meta refresh + 按钮触发拨号，任何扫码器可用）；3) 新增免登录路由 `GET /qrcode/call/<digits>`（数字校验 + 号码掩码）并登记进 `PUBLIC_PREFIXES`；4) 前端文案与历史记录同步（历史项改存 `call` 字段，pref/hist key 升 v2）。**验证**：测试 13→17 条全绿；光栅化 + OpenCV 解码 3 模式 × 4 纠错等级 × 3 尺寸共 **36/36** 全部还原（cv2 对个别像素尺寸采样不稳，脚本内加多尺度重试）。新增坑点 7.44（`tel:` 在微信不拨号）。 |
| 2026-09-14 | 挪车码默认改为「网页中转」 | 用户实测不要名片（扫码后只是名片、还要再点一次「呼叫」）、`tel:` 也不能拨号。1) **后端** 默认 `mode` 由 `vcard` 改为 `page`，`MODES` 顺序调为 page/tel/vcard，docstring 同步；2) **前端** 选项顺序改为「网页中转（推荐）/直接拨号/联系人名片」，默认选中 page，说明文案重写（写明 tel 在微信只显示文本、名片要多点一次）；3) **旧数据兼容**：pref key 升 v3，新增 `fixCall()` 把偏好与历史里存到的 `vcard` 一律按 `page` 处理，否则用户刷新后仍是名片、看起来没生效。测试断言同步改默认，17/17 全绿。 |
| 2026-09-14 | 二维码彩色化 | 用户要求二维码做成彩色。1) **配色** `THEMES` 扩为 5 套（清新绿/沉稳蓝/活力紫/暖阳橙/墨黑），每套含标题条渐变 `from/to`、码点深色渐变 `dot1/dot2`、背景浅渐变 `bg1/bg2`、提示文字 `ink`；2) **码点与定位点**改用主题深色对角渐变，渐变必须 `gradientUnits="userSpaceOnUse"` + 码区坐标（否则每个码点各自渐变、明暗跳变），定位点白环与静区保持纯白（卡片模式在码区额外铺一层白底）；3) **卡片底**改同色系浅渐变、提示文字用 `ink`；4) **`render_png()`** 同步彩色，按模块对角位置 `mix_color()` 取色，保证导出图与预览同色；5) 无文案的纯码也彩色；6) 新增 `luminance()` / `mix_color()` / `_defs()`；7) **前端** 配色按钮 3→5 并加说明。测试 17→20（新增主题渲染、配色对比度自检、PNG 上色）。**验证**：自写 SVG 光栅化（渐变取最差对比端）+ OpenCV 解码，5 主题 × 4 纠错等级 ×（3 尺寸 + 纯码 + PNG）共 **80/80** 全部还原。新增坑点 7.45（彩色码的对比度约束）。 |
| 2026-09-14 | 挪车码功能收敛 | 用户要求砍掉多余功能。1) **后端** 删掉名片与直拨：`build_content()` 只生成中转页 URL，删除 `_VCARD_NAME` / `MODES` / `mode` 参数（传了也忽略，`mode` 字段固定返回 `page` 兼容旧调用）；号码 `normalize_phone()` 不再补 +86，统一 6~15 位纯数字，中转页 `tel:` 同步不带区号；2) **前端** 删掉「扫码后做什么」整块（callSeg / CALL_HINT / fixCall / state.call）、「自动添加 +86 区号」勾选（intlChk / state.intl）、「下载 SVG」与「复制号码」两个按钮（saveSvg / copyPhone），底部只留「保存图片」；pref key 升 v4、hist key 升 v3，历史项不再存 call/intl 字段。测试同步（mode/intl 改为「传了也无效」，号码断言改无区号），19/19 全绿；JS `node --check` 通过。 |
| 2026-09-14 | 挪车码：码点样式可选 + 真多色 | 用户反馈「彩色的概念并不是单一多色」——之前每套主题只是同色系深浅渐变，本质仍是单色。1) **真多色**：`THEMES` 每套加两个跨色相深色 `m1/m2`，码点渐变改四色停 `dot1→m1→m2→dot2`（全为深色，如深蓝→深紫→深玫红→深青，亮度均 ≤120），标题条加中间色 `accent`（`from→accent→to`）；新增第 6 套配色「流光」rainbow；2) **样式可选**：新增 `style` 参数与前端 `styleSeg`（`multi` 多色渐变默认 / `grad` 同色渐变 / `solid` 纯色），新增 `_dot_stops()` 统一生成色停、`dot_color()` 供 PNG 取色，SVG 与 PNG 共用同一套色停；非法 style 报 400；3) **前端** pref 升 v5、hist 升 v4（新增 `style` 字段），兜底 PNG 请求补上 `theme`/`style`（此前固定绿色）。测试 19→21（新增样式切换、取色一致性、非法 style），全绿；解码验证 **SVG 144/144 + PNG 72/72**（最差对比取色）。规范 7.45 补充「多色≠提亮、只在深色区内换色相」。 |
| 2026-09-14 | 挪车码：回退传统黑白，取消配色与码点样式 | 用户要求取消配色和码点样式、恢复传统样式。1) **后端**：删除 `THEMES`/`THEME_ORDER`/`STYLES`/`_dot_stops()`/`dot_color()`/`mix_color()`/`luminance()`/`_defs()`（连带 `uuid` import），只留 `INK/PAPER/BAR_INK/HINT_INK` 四个常量；`render_svg()` 与 `render_png()` 去掉 theme/style 参数，码点纯黑、背景纯白、标题条改深色、提示行灰字；卡片不再铺主题浅色底。2) **兼容**：旧请求带 `theme`/`style` 不再报 400，直接忽略（测试 `test_theme_and_style_params_ignored`）。3) **前端**：删掉「配色」「码点样式」两组分段按钮与 `.seg` 样式、`state.theme/style`、历史/偏好里的对应字段与 `bindSeg/setSeg/curSeg`，分区标题改「参数」。4) **验证**：测试 21→19（删配色相关，新增黑白断言与废弃参数兼容），全绿；解码验证 4 纠错等级 ×（PNG + 卡片 SVG + 纯码 SVG）**12/12**。规范 7.45 改写为「回退黑白 + 若将来再做彩色必须满足的 6 条」。 |
| 2026-09-14 | 挪车码：码点形状也回归传统 | 用户要求「二维码样式也要回归传统」。删除圆角美化：码点圆角 `_DOT_RX`、定位点圆角 `_FINDER_RX`、留缝 `_DOT_INSET` 三个参数，以及只为画圆角定位点而存在的 `_finder_boxes()` / `_in_finder()`；新增 `_dots_path()` 把同一行连续的黑块并成一个矩形、整块码合成一条 `path`（纯码与卡片两条分支共用，支持传入原点偏移），元素最少、体积最小，定位点也不再单独美化。卡片只保留外框圆角与深色标题条（属卡片装饰）。测试 19→20（新增 `test_dots_are_plain_squares`：断言三个圆角常量已不存在 + SVG 里除卡片外框外没有小圆角），全绿；解码验证 4 纠错等级 ×（PNG + 卡片 SVG + 纯码 SVG）**12/12**。规范 7.45 补充「码点形状回归标准方块」。 |
| 2026-09-22 | 删除二维码（挪车码）模块 | 用户决定不再要这个功能，整体删除。1) **文件**：`git rm -r` 删掉 `routes/qr.py` 与 `二维码/`（index.html、manifest.json、icon-192/512.svg）；2) **app.py**：移除 `('routes.qr', 'qr')` 模块注册、`qr_bp` 变量与分支、`_LOADED_MODULES` 里的 `qr`，以及 `/qrcode`、`/qrcode/`、`/qrcode/manifest.json`、`/qrcode/icon-192.svg`、`/qrcode/icon-512.svg` 五个静态路由，`PUBLIC_PREFIXES` 去掉`/qrcode/manifest`、`/qrcode/icon`、`/qrcode/call` 三条；3) **routes/utils.py**：`KNOWN_MODULE_DIRS` 去掉 `'二维码'`（否则备份清理逻辑会盯着一个不存在的目录）；4) **导航/index.html**：删掉「挪车电话码」入口卡片；5) **测试**：删除 `tests.test_app.TestQRCode`（18 条）与仅它用到的 `_png_colors()` 辅助函数、`re`/`struct`/`zlib` 三个 import；6) **规范**：目录树去掉 `routes/qr.py` 与 `二维码/`，删除二维码专属坑点 7.40（PNG 双向放大）、7.43（留缝/大圆角扫不出）、7.44（`tel:` 在微信不拨号）、7.45（彩色回退黑白），保留通用的 7.41（新模块目录必须登记白名单，示例改为泛指）与 7.42（dulwich 推送）。**验证**：`python -m unittest discover -s tests` 与删除前同为 4 个失败（`TestAppBasics` 3 条 + `test_pull_without_token`，都是登录拦截导致的历史遗留，与本次无关，已用 `git stash` 对比基线确认）；`/qrcode` 相关路由全部不存在。|
| 2026-09-22 | 删除轨迹（开车轨迹）模块 | 用户决定不再要这个功能，整体删除。1) **文件**：`git rm -r` 删掉 `routes/track.py` 与 `轨迹/`（index.html、manifest.json、icon-192/512.svg），本地残留的 `track.db` / `track.log`（`.gitignore` 忽略、未入库）一并用 `Remove-Item` 清掉；2) **app.py**：移除 `('routes.track', 'track')` 模块注册、`track_bp` / `init_track_db` 变量与赋值分支、`_LOADED_MODULES` 与 `_SAFE_INITS` 里的 `track`，`PUBLIC_PREFIXES` 去掉 `/track/manifest`、`/track/icon`；3) **routes/utils.py**：`FOLDER_MAP` 去掉 `'track': '轨迹'`，`KNOWN_MODULE_DIRS` 去掉 `轨迹`；4) **routes/backup.py**：备份清单 `DB_PATHS` 与「恢复备份」的 `db_targets` 各去掉一条 `track.db`；**routes/deploy.py**：`KNOWN_DB_DIRS` 去掉 `轨迹`，数据库恢复接口 `allowed_prefixes` 去掉 `轨迹/`；5) **导航/index.html**：删掉「开车轨迹」入口卡片；6) **规范**：目录树去掉 `routes/track.py` 与 `轨迹/`。（本模块没有独立的坑点条目，也没有单元测试，故这两处无需改动。）**验证**：`python -m py_compile` 通过；`python -m unittest discover -s tests` 40 条 / 16 失败，与删除前完全一致（这 16 条是登录拦截导致的历史遗留，已用 `git stash` 对比基线确认）；全库检索 `track` / `轨迹` 已无残留；线上 `/track`、`/track/manifest.json`、`/api/track/active` 与任意不存在路径表现一致（返回登录页），`/nav/manifest.json` 正常 200。|

---

> **核心一句话**：提交代码前用 `py_compile` 检查语法、用 `test_client` 测接口、推代码后等部署完成再验证。任何「小改动」都可能让服务器挂掉。
